# Daedalus — Task List of Record

**Persists across days.** The drain-loop's task source. Unblocked items the cycle picks up; blocked-on-xian items surface (Calliope's verified sweep promotes them into the attention rollup — the cycle batches, doesn't act).

Created: 2026-06-21 (Phase 2 launch). Format per `duty-cycle-klatch-v0.2.md`.

## Unblocked (cycle can advance)

- [ ] **Composition gesture implementation** — the 1.0 critical-path blocker. Spec: `docs/ux/spec-composition-gesture.md`. Build on `claude/daedalus`; pushing branch work is unblocked, **merge to `main` is review-gated (blocked-on-xian)**. **Corrected scope (6/21):** substrate already mostly built — `channels` has `type`+`mode` (no migration), routes create+assign, `parseMentions`/`resolveMentions` exist, an earlier klatch form exists. Work = evolving the front-door to spec. Sub-phases:
  1. ✅ **Data model** — no migration needed (`mode` column = spec's orchestration_mode). Done: atomic roster at creation (`createChannel(...entityIds)` + route validation) — kills stray-default-entity wart.
  2. ✅ **Spine increment 1** — dual New Chat/New Klatch affordance + Purpose label. Tests green (server 1096, ChannelSidebar 18).
  3. ✅ **Increment 2** — agent picker polish (typeahead + chips + roles-first) merged (`07bda25`). Iris confirmed conformant.
  4. ✅ **Default-project increment — DONE on `claude/daedalus` (`0719adc`), awaiting xian+Iris review/merge** (`docs/ux/decision-klatch-project-optionality.md`). Sentinel (`project_id` null = default project, no migration). (a) ✅ form defaults to "First project" + select hidden until real projects exist (klatch always creatable); (b) ✅ null-project channels (chats+klatches) render in the **"First project"** group — chats over klatches; flat/headerless for singleton; pinned+collapsible header when real projects exist; (c) ✅ **Round 7 inverted** — reconciled SidebarRedesign+ChannelSidebar (Unassigned→First project, klatches included, singleton-flat), +2 new tests, removed round33b T2.4 subtitle. Argus flagged (`daedalus-to-argus-default-project-round7-inversion-2026-06-22.md`); Iris review requested (`daedalus-to-iris-default-project-built-review-2026-06-22.md`). Isolated green: sidebar 30/30. One open Iris call: #general pinned-top vs nested under singleton CHATS label.
  5. ✅ **Cross-reference surface — DONE on `claude/daedalus` (`e2568ee`), awaiting review/merge.** 1-1 role chat shows the klatches its agent is also in. Server: `getKlatchesForEntity` + `GET /entities/:id/klatches` (+tests). Client: `fetchKlatchesForEntity` + `CrossRefStrip` component ("Also in: #klatch" links) below the header. Iris review requested (`daedalus-to-iris-crossref-surface-review-2026-06-22.md`); placement/styling + `#general`-inclusion are her calls. Also flagged client tsc baseline (17 errors, mostly pre-existing) to Argus (`daedalus-to-argus-client-tsc-baseline-2026-06-22.md`).
  6. ✅ **Clone-from-klatch — DONE, merged to main (`a313ab2`, 6/28).** Iris-reviewed Conformant; verified live in MAXT Session 03.
  7. ✅ **@mention — DONE, merged to main (`aaca51b`, 6/28).** Shipped wider than the title: the autocomplete already existed (directed-mode only); the actual work was making an @mention **override any klatch mode** per spec §5, plus ungating the dropdown to any 2+ agent klatch. Iris verified the override live in MAXT-03.
  8. [ ] **Paths B + C — DECIDED 2026-08-10, still unbuilt. No xian call outstanding.** *(Corrected 9/2 by Daedalus; Calliope flagged this item as stale in `calliope-to-daedalus-cc-team-xian-correction-paths-bc-already-decided-2026-09-02.md` and she was right — the text below previously said "Needs an explicit xian call," which had been false since 8/10.)* The decision is `docs/ux/spec-composition-gesture.md` §11a (`851e10c`): **Path B (JIT import) SCHEDULED**; **Path C → "continue existing role" SCHEDULED**; **Path C → "new agent from scratch" HELD** pending a framing that visibly separates "mint a new entity" from "bring in an existing agent" (listing them as peers is the interchangeability `PREMISE.md` rejects — held, not descoped).
     - **§11a's blocker-clearance is itself stale, and this is why Path B has not started.** §11a:239 clears Path B on "imports now mint a real entity via guess-and-confirm." **Verified 9/2 against the shipped client:** `packages/client/src/api/client.ts:621-634` sends `sessionPath`/`channelName`/`forceImport` and **no entity fields at all**, so `resolveImportEntity` returns `'default'` and the import lands on `default-entity`. The server half is correct; the client never asks. A JIT import built today would do the exact thing §11a calls "the exact broken thing" and would *add* to the backfill population. **Path B's real dependency is Iris's import confirm step** (`docs/ux/import-confirm-step-scope-2026-08-09.md`, 21 days idle as of 8/30), not continuity #2/#3. Independently measured the same day by Theseus (`docs/research/friday-import-entity-binding-2026-09-02.md`).
     - **Path C → "continue existing role" is genuinely unblocked and genuinely small** — it binds an existing entity to a new channel via the picker and never touches the import path. This is the buildable half of item 8 today.
     - **Note (unchanged):** Path B is inline-import *UX* — it does **not** close the continuity gap (`docs/plans/composition-continuity-gap-2026-07-19.md`). Iris's open Q from 6/21 (nameless vs. name-on-import) was answered by §11a: **name on import**.
     - Full reasoning: `docs/plans/entity-backfill-scoping-2026-09-02.md` §5.
- [ ] **Finding 1 dedup logic (UUID re-import matching)** — Iris's UX answered (`iris-to-daedalus-uuid-matching-ux-reply-2026-06-20.md`): project match → silent attach + toast; channel match (UI) → inline "View existing / Import as new copy"; channel match (MCP) → 409 with `reason` + `existing_channel_id`. **Partially audited against shipped code 8/18 — "Implementable now" was too flat. Per branch:**
  - **(c) MCP 409 — NOT IMPLEMENTABLE AS SPECCED.** MCP registers exactly four tools — `list_channels`, `get_context_package`, `get_manifest`, `reflect` (`mcp/server.ts:496,555,613,651`). **There is no import tool on MCP**; the surface this branch targets does not exist. Import is HTTP-only. Either drop the branch or first decide whether MCP gets an import tool at all — an xian/Iris scope call, not a build task.
  - **(b) UI channel match — BUILT, but diverges from the spec's affordances.** `ImportDialog.tsx:432-478` renders an inline conflict state ("Already imported" + existing channel name/message count + a "messages added since import" warning) with two actions: **"Replace existing"** and **"Import as new"**. Iris specced **"View existing"** / "Import as new copy". Shipped offers a *destructive* action where she specced a *navigational* one. The HTTP 409 backing it (`routes/import.ts:186-199`) returns `error: 'duplicate'` + `existingChannelId`/`existingChannelName`/`existingMessageCount`/`hasNewMessages` — richer than the specced `reason` + `existing_channel_id`, and camelCase not snake_case. Tests exist (`ImportDialog.test.tsx:800` "Import as new calls forceImport"). **Needs Iris's read on the Replace-vs-View divergence before anyone "conforms" it** — do not silently rename a shipped destructive action.
  - **(a) project match → silent attach + toast — NOT VERIFIED this fire.** Klatch-package import does reuse an existing project by id (`klatch-import.ts:236-240`, `reusedProject`); whether the claude.ai path silently attaches *and toasts* is unchecked. Verify before building.
- ✅ **Round 31b cosmetic follow-ups (from Argus) — ALL THREE CLOSED. Reconciled 8/18; this entry was stale and pointed the wrong way.** (1) ✅ **RESOLVED** — `package-builder.ts:58` already correctly maps `klatch`→"Klatch" (the old "claude.ai" mislabel is gone; verified 6/22). (2)+(3) ✅ **BUILT 2026-05-11 in Round 32 (`ef613fc`, "import gating + empty-entities auto-attach (Argus 31b follow-ups)")** — and shipped the **opposite** contract from the 4/28 decisions this entry was still carrying:
  - **(2) `format_version` — hard-gated, NOT permissive.** `klatch-import.ts:188-199` rejects missing/malformed/unsupported versions with `400` + structured `versionMismatch: { formatVersion, supportedVersions }`, before the import transaction opens (no partial import). Round 32's stated reason overrides the 4/28 forward-compat argument: *the import path materializes into the DB, so accepting an unrecognized version silently drops fields we can't model — the worst kind of fidelity loss.* Mirrors the export side's long-standing `negotiateFormatVersion`.
  - **(3) empty `entities: []` — auto-attaches `default-entity`, NOT left empty.** `klatch-import.ts:281-288` `INSERT OR IGNORE`s the seed entity, matching `createChannel`. Avoids the "imported but un-exportable" user-trap the 4/28 note accepted.
  - **Canonical now:** `docs/plans/STEP-10-PHASE-1-PACKAGE-FORMAT.md` §"Import-side validation (Round 32, 2026-05-11)". Covered by `round32-import-gating.test.ts` (8 tests) + `import-hardening.test.ts` (10); **re-ran 8/18: 18/18 green.**
  - **Why this sat wrong for ~3 months:** the 4/28 decisions were carried verbatim into this file at Phase 2 launch (`180e5d9`, 6/21) as pending "tiny doc/comment touches" and never reconciled against Round 32, which had already landed 5/11. Item (1) was verified against code on 6/22; (2) and (3) were not. **A fire that had executed this entry as written would have regressed a shipped, spec-documented, test-covered contract** — deleting the version gate and the auto-attach. Nothing to do; do not "finish" this item.
- ✅ **SidebarRedesign chats-before-klatches flake — DIAGNOSED 6/21, no product fix (correction to Argus).** My earlier "query `created_at` tie" root-cause was **wrong**: the client already groups chats/klatches into separate arrays and renders chats first (`ChannelSidebar.tsx:152-155,194`), so the order is *structurally guaranteed* — no query/data tie can flip it. Ran ~13× isolated: ~1-in-5–10 flake with the "6856ms" smell → **test-side timing**, Argus's domain (corrected: `daedalus-to-argus-sidebar-rootcause-correction-2026-06-21.md`). Optional within-type tiebreak nicety (`byLastActivity`/query `rowid`) deferred — unrelated to the flake. ~~getChannelEntities tie~~ ✓ fixed (increment 1).
- ✅ **type/roster coherence (chat+multi)** — DONE on `claude/daedalus` (`0eb0ec9`, awaiting review/merge). Route-level 400 for chat + 2+ agents. **Narrowed** from the 2-invariant flag: klatch+empty is a valid 1-agent klatch (NOT enforced — would break round7 + create-then-add). +2 tests, server 1099/1099. Argus PINs: chat+multi flips to rejected, klatch+empty stays allowed (`daedalus-to-argus-invariants-revision-2026-06-21.md`).
- ✅ **SDK bump — DONE 7/5: `^0.96.0` → `^0.110.0`** (`0395c4b`; Argus sweep #14 raised the target past the old 0.104.1). Landed with the AVAILABLE_MODELS overlay (Sonnet 5 + Fable 5). **Real-stream verification still outstanding** — route tests mock streaming and this worktree tree-walks to the original repo's older SDK, so confirm on the next live run. *Historical assessment (sweep #13, 0.104 target):* **✅ Changelog + code-review risk assessment DONE 6/22 (Fire 5): LOW-RISK** — see `docs/plans/SDK-BUMP-0.96-to-0.104-RISK.md`. No breaking changes / no `messages.stream()` signature change / no abort-behavior change in the range; and Klatch consumes the stream via the **high-level helper** (`stream.on('text')` + `finalMessage()`), which insulates it from the additive raw-event changes (mid-conv system blocks @0.100, thinking-token-count @0.98). **Verification gap (Fire 2) still holds in principle** (route tests MOCK streaming, so only a real stream confirms) — but it's now a ~5-min happy-path **confirmation**, not a breakage hunt. Steps + the one watch item (the `beta.messages.stream`/compaction path) are in the doc. Bump itself stays for xian / a fresh session (needs the real-stream verify); gates the `DEFAULT_MODEL` 4.8 flip + unlocks fable-5/mythos-5 (@0.103, the suspension-fallback models).
- ✅ **Model discovery/validation unification** — IMPLEMENTED on `claude/daedalus` (`84e7d71`), awaiting review/merge (sweep #13; supersedes the "add 4.8" band-aid). ModelId→`string` alias, validate against discovered `/api/models` set, capability-gating from metadata, AVAILABLE_MODELS→overlay with corrected fallback effort. Server 1099/1099, client 199/199, zero new tsc errors. Test-coupling resolved via fallback-correctness (NOT a global setup seed — that broke round13; learning recorded in `docs/plans/MODEL-VALIDATION-UNIFICATION.md`). Argus test round (new-behavior: accepts-discovered-4.8, capability-from-metadata, picker↔validation invariant) to follow — seed locally per the design doc. `DEFAULT_MODEL` 4.7→4.8 flip stays separate (Blocked-on-xian).
- [ ] **Pre-beta vocab copy sweep** (Iris flagged 6/20) — `entity`→`agent/role`, `channel settings`→`klatch/chat settings` across the rest of the UI (composition surface already uses correct vocab). Low effort; needs to land before beta invites. Coordinate with Iris so I don't collide with her in-flight copy work.
- [ ] **Recall-probe support for arm N** (Theseus's build, my surface) — **8/18 STOP: my two halves are done; the remaining blocker is his content.** ✅ Pair-count guard in `probe-recall-tool.mjs` (throws when `leadPairs > FILLER_LEAD` or `gapPairs > filler`, before the first row — closes his §4 silent truncation, which both N1 and N2 would have hit first). ✅ `scripts/verify-filler-constraints.mjs` — hard-checks codeword / restriction overlap / list distinctness / arm-`ask` match on the filler corpora, reports retry exposure rather than ruling on it; negative-tested against three doctored copies via `--probe=`. ✅ The unclamped-offer path (`recall.ts:858-882` offers wider than `:748` returns) covered by +3 tests in `round56-recall-expand.test.ts`. **Two things I own that are NOT done:** (a) **no `--dry` on M has been run across the guard edit** — this fire could not start the scratch server (approval unavailable non-interactively); the guard is proved inert by enumerating all 12 arms against the list lengths, which is a proof but not the run Theseus asked for, so **a `--dry` on M is the first action of any N build**; (b) the 10 new `FILLER_LEAD` pairs are **deliberately not mine** — content interacts with his query and recogniser. **Design ruling filed:** no objection to N1-before-N2, `leadPairs: 15` (leading 28) is correct over 14 (leading 26) because exact equality is unreachable by parity and 28 makes cost predict *against* M's result; **`leadPairs` must stay ≤ 16** or N1 inherits N2's truncation variable.
- [ ] **Mail drain + log upkeep** (continuous) — keep `docs/mail/` at inbox-zero per Mail Handling; move closed threads to `docs/mail/read/`; cycle log + session log turn-by-turn.

## Blocked-on-xian (cycle surfaces, doesn't act)

- ✅ ~~**Force-push approval to sync `origin/claude/daedalus`**~~ **RESOLVED 6/21** — xian approved; force-pushed (`aabca4d…c42c5f1`), branch synced (origin == local == main). Branch pushes work again; code increments resumed (invariants enforcement pushed `0eb0ec9`).
- **`DEFAULT_MODEL` flip 4.7 → 4.8** — **product decision (yours), morning candidate.** Like the 4.6→4.7 flip that got sign-off. Priority raised by the 6/12 Fable5/Mythos5 government suspension (Anthropic's recommended fallback is Opus 4.8; Klatch defaults to 4.7). Post model-validation-unification, 4.8 is already *selectable/validatable* (validation is dynamic now — no AVAILABLE_MODELS add needed); the **SDK bump `0.96→0.104` is the technical prereq to actually run 4.8** (4.8 support landed @0.100.0). So: bump the SDK (Unblocked, I'll do it) → then flip is a one-line product call.
- **Overnight branch increments awaiting your review/merge** — increments 1, 2, invariants, model-validation already ✓ merged to `main`. Anything the cron builds tonight (default-project increment first, then cross-ref / clone / @mention) lands on `claude/daedalus` **branch-only** per discipline — review + merge them in the morning. I'll keep them coherent + individually green.
- **Round-trip MAXT** (assigned to Theseus 4/28, `daedalus-to-theseus-roundtrip-maxt-2026-04-28.md`) — behavioral round-trip testing; parked, needs xian to direct Theseus. **Note:** the composition spine is now demoable on main — Theseus is also green-lit for agent-experience testing on it whenever you launch him.
- ✅ **Duty-cycle calibration Q — ANSWERED (xian, 2026-06-22 AM):** holding was prudent → hold UX-delicate / shared-test-touching increments for fresh + coordinated context; autonomy = mechanical/bounded + research/prep. Also: overnight watch may be **sparse** (≥1 check evening→morning, not hourly all night). [recorded: memory `duty-cycle-overnight-calibration`]
  - _Original Q (overnight 6/22):_ what's the right autonomous-build boundary? Tonight I **deferred** the (fully-specced) default-project increment because it's UX-delicate, edits Argus's Round 7 tests, and wants Iris's rendering review — judged better for fresher context / Iris+Argus active, since branch-review is the net either way. Is that the right call, or do you want me building such increments autonomously overnight (branch-only) and letting morning review catch UX/test issues? Your answer recalibrates how aggressively the cron builds vs. queues. (Mechanical/bounded work I'll keep doing autonomously regardless; this is specifically about UX-delicate + shared-test-touching increments.)
  - **UPDATE (Fire 6, ~02:17 PT):** Iris resumed 05:00-clock and **closed my default-project mechanism thread** (sentinel accepted, no new Qs) — and her decision doc already specs the rendering. So the increment is now **spec-complete + design-aligned**; the "wants Iris active" deferral reason is resolved. **The only remaining gate is your autonomous-build-boundary answer.** The Round 7 test inversion can be flag-and-review with Argus (he's not active yet). Net: a "yes, build it" from you → I build a fully-ready, fully-specced increment (branch-only, you+Iris+Argus review the result). A "no, keep queuing" → it waits for a fresh session. Either is fine; just want you to know it's teed up, not half-baked.

## Watch items (cycle monitors; triggers action when condition met)

- ✅ ~~**Mode-key naming reconciliation**~~ **RESOLVED 6/21** — Iris confirmed (`iris-to-daedalus-composition-spec-ack-2026-06-21.md`): store the internal code keys `panel|roundtable|directed`; §10 vocab table is canonical (`panel`=internal, `Broadcast`=user-facing). No key rename.
- **Composition-spec → demo-able** (shared with Calliope's watch list) — when implementation produces something demo-able / client-legible (a working flow, screen recording, transporter-device artifact), note it so Calliope can trigger her Janus one-liner. Trigger lives downstream of my own implementation.

## Recurring items (START dispatcher promotes when `next_due ≤ today`)

| Item | Cadence | next_due | last_completed | Notes |
|---|---|---|---|---|
| Rebase `claude/daedalus` against `main` | daily | 2026-06-22 | 2026-06-21 | Worktree discipline (v0.2): long-lived branch rebases daily, merges to main on wrap. |

## Notes

- Tandem with Argus (`:43`; I'm at `:17`). Who-touches-what: I implement composition surface; Argus writes extended-coverage tests against my new surfaces as they land. Coordinate via COORDINATION.md + mail. Mutual-assessment "what surprised me" memo after a few days on cycle.
- Most composition work lands on `claude/daedalus` and is push-unblocked; the *merge* is the xian-gated step. Don't sit passively (5/12): when blocked on review, drop to the next unblocked item (Finding 1 dedup, round31b follow-ups, vocab sweep).
