# Argus — Task List of Record

**Persists across days.** The drain-loop's task source. Unblocked items the cycle picks up; **Blocked-on-xian** items surface to the attention rollup (the cycle batches, doesn't act) via Calliope's verified sweep — keep this list true so her sweep finds real items.

Updated: 2026-06-21 (Phase 2 launch — initial v0.2 task list)

## Unblocked (cycle can advance)

- [ ] **Test rounds following Daedalus** — extended-coverage as he lands composition increments. *Increment 1 (atomic roster + dual affordance, `7d42822`) DONE 6/21 → `composition-gesture-extended.test.ts` (8 tests). My Fire-1 invariant finding drove enforcement: Daedalus shipped `chat`+multi→400 (`d4fc8a5`); I **flipped** that PIN allowed→rejected + added a 1:1 boundary test. `klatch`+empty kept permissive (his round7 catch — valid 1-agent klatch). Suite 1107 server / 198 client green. **Increment 2** (agent picker, `07bda25`) coverage **LIVE 6/21** (`composition-picker-extended.test.tsx`, 5 tests: cap / chip-removal / handle-filter / partition / end-to-end roster). Briefly parked, then **un-parked** via the test-timing hardening — `fireEvent` + per-test `{ timeout: 15000 }` on the two heavy tests. Client suite green **204** across 5 clean full-suite runs.* **Remaining as he ships them:** setup-panel fields, agent picker (existing / JIT-import / new session), Broadcast/Roundtable/Directed behavior, @mention composes-with-all-modes, clone-from-existing-klatch. He owns ChannelSidebar dual-affordance tests; I own the new-surface behavioral/edge coverage.
- [x] **Client-suite load-flake — RESOLVED 6/22 (global `testTimeout: 15000`).** Investigation showed it's **suite-wide**, not per-file: an ImportDialog-only `vi.setConfig(15000)` fixed *that* file but the failures just moved to SidebarRedesign / ExportReviewPanel (still timing out at the 5000ms default under load). So the right-sized fix is a global `testTimeout: 15000` in `packages/client/vitest.config.ts` (a **complement** to singleThread, not the "cop-out" — the investigation justified it). Realistic single-run (incl. busy-machine, the case that bit Iris) **5/5 green**; full suite **1112 server / 204 client**. Reverted the redundant ImportDialog/`setup.ts` experiments. **Residual (operational, not code):** under *artificial* 2–3× concurrent vitest saturation the suite still flakes (longer timeouts are a wash there) — kill stray vitest procs / run one suite at a time before flake checks. The deeper render-speed / true-isolation work is optional future polish, not blocking. Routed reasoning to Daedalus.
- [ ] **Mail drain** — keep `docs/mail/` at inbox-zero per Mail Handling; respond/act/route same turn; `git mv` closed threads to `read/`; push mail commits to `main` immediately. (Continuous.)
- [ ] **Cycle log + session log upkeep** — turn-by-turn during xian-present sessions; brief entry per substantive fire; batch no-ops. (Continuous.)
- [x] **Flaky-test triage — SidebarRedesign DOM-order** — **FIXED 6/21.** Daedalus corrected his root-cause (structurally guaranteed order; test-side timing only). Hardened all 4 tests in the `chats-above-klatches` block to settled-render (`findBy*` before any synchronous index). Verified **0/10 isolated** (was ~1-in-5–1-in-10) + green across 5 full-suite runs. Same hardening pass un-parked the picker coverage.
- [ ] **AAXT continuation candidates** (5/18 green-lit, UI-as-context line) — ProjectSettings (F5.1), EntityManager, MessageList (F1.4). Larger initiative; pick up in a quiet test-round window, not driven without one.

## Blocked-on-xian (cycle surfaces to attention rollup; doesn't act)

- **`DEFAULT_MODEL` flip `claude-opus-4-7` → `claude-opus-4-8`** (low-urgency decision) — Opus 4.8 (released 5/28) is Anthropic's recommended ceiling, reinforced by the 6/12 Fable/Mythos government suspension. Adding 4.8 to `AVAILABLE_MODELS` is routed to Daedalus (code, his lane); flipping the *default* is a product decision like the 4.6→4.7 flip (which got Calliope/xian sign-off). Not urgent — 4.7 is not deprecated. Gates on Daedalus's SDK bump + 4.8 add landing first. (Sweep #13.)
- *(merged-and-clear)* — vocab-fallout + round25 flake fixes merged to `main` (`1a29830`, 6/21); composition extended coverage on `claude/argus` (`d38a89f`, ready to merge).

## Watch items (cycle monitors; one-line outbound when condition met)

- ~~**Daedalus `getChannelEntities` ordering finding**~~ **RESOLVED 6/21** — Daedalus fixed on `claude/daedalus`: `ORDER BY ce.added_at ASC, ce.rowid ASC` (rowid preserves user-picked roster order — better than my `e.created_at/e.id` suggestion; his atomic-roster create made it a live common-path bug). Lands with his composition merge. Thread closed both sides.
- ~~**Model discovery/validation split**~~ **CLOSED 6/21 — full loop, same day.** xian-flagged ("tops out at 4.7… brittle") → my sweep #13 finding + migration sketch → Daedalus designed + implemented the unification (ModelId→string, runtime validation vs discovered set, capability-from-metadata, AVAILABLE_MODELS→overlay; merged to main) → **my new-behavior round shipped** (`model-validation.test.ts`, 5 tests: discovered-model-validates / garbage→400 / offline-fallback-to-overlay / capability-from-`effort[]` / **picker↔validation consistency invariant**). Cache seeded file-local via `_setModelsCacheForTest` + cleared in afterEach — round13 isolation preserved (11/11). Suite **1112 server / 204 client** green. Only residual: `DEFAULT_MODEL` 4.7→4.8 flip (Blocked-on-xian, low-urgency product call).
- ~~**Client-suite fragility (singleThread timing)**~~ **RESOLVED via test-convention 6/21** (no config overhaul needed — downgraded the finding to Daedalus). The cluster (SidebarRedesign flake + parked picker) is closed by: `findBy*` for accordion-settle flakes; `fireEvent` + per-test `{ timeout: 15000 }` for heavy interaction tests; and **kill stray vitest procs before full-suite flake checks** (much of the 11–36 cascade was my own concurrent runs adding machine load, not an inherent ceiling). Both landed green. **Superseded by the 6/22 full resolution:** investigation showed the flake is **suite-wide** (the per-file ImportDialog fix just moved the failures to SidebarRedesign/ExportReviewPanel), so a global `testTimeout: 15000` (complement to singleThread, `packages/client/vitest.config.ts`) landed — realistic single-run **5/5 green**; full suite 1112/204. So the config option I'd called "likely unnecessary" turned out to be right-sized after all (the investigation justified it). **WATCH (xian-directed 6/22 — keep an eye on it, don't chase):** under *artificial* 2–3× concurrent vitest saturation the suite still flakes (a longer timeout is a wash there — slow test holds the singleThread process longer). NOT a CI condition today. Trigger a closer look (render-speed / true-isolation) only if it ever bites a *realistic* run — CI running suites concurrently, or recurring busy-machine failures.

## Recurring items (START dispatcher promotes when `next_due ≤ today`)

| Item | Cadence | next_due | last_completed | Notes |
|---|---|---|---|---|
| Weekly intel sweep | weekly | 2026-06-28 | 2026-06-21 | AI-landscape sweep → curated review in `docs/intel/`. **Sweep #13 done 6/21** (`2026-06-21-sweep-curated.md` — curated 4 automated sweeps 5/25–6/15; live DB audit + NSA MCP audit both clean). Lives here, not a separate cron — the hourly cycle's START promotes it. |

## Notes

- Tandem with **Daedalus** (`:17`). Building mode = the engine (test rounds chase his commits); planning mode (current) = the cycle keeps us current. Lower-priority-unblocked beats higher-priority-blocked (don't-sit-passively, 5/12).
- Stagger: Calliope `:13`, Daedalus `:17`, Argus `:43`.
- Weekly intel sweep is a recurring row (cadence-aware via START), not a separate cron — the standard hourly cycle handles the once-a-week shape with no special-casing.
