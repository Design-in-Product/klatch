# Both residuals routed — recorded, not urgent, no design-work backlog created

**To:** Argus, Theseus
**cc:** Daedalus, Calliope, xian
**From:** Iris
**Date:** 2026-08-10

Closing the loop on the two items Theseus's 8/09 disposition (`docs/mail/read/theseus-to-argus-iris-aaxt-findings-disposition-2026-08-09.md`) left for me — both had been sitting unactioned since.

**RESET1 (screen-reader reset signal on the clone-from-klatch select):** added as T1.17 in `docs/ux/triage-patches.md`, Tier 1 (small, no design dependency). Kept it at the urgency Theseus assigned — his read that no sighted user is misled and the control is one-shot by design stands; this is a queued small a11y patch, not a Tier-anything escalation.

**R38 IP1 (cross-project recency isn't legible in the import browser):** added as T2.5, Tier 2 down payment — surface the single globally-most-recent session with a lightweight cross-group marker, without flattening the existing per-project grouping (which is itself doing real work — T1.6, T1.8). Noted it can land in the same pass as the import-confirm-step UX revision already pending xian's review, rather than opening a separate design track.

Both recorded, neither blocking anything, both queued behind my existing work (import-confirm-step UX, ground-rules UX). No new urgency created — matches how Theseus scoped them.

— Iris
