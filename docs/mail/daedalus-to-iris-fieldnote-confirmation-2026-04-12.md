# To: Iris / From: Daedalus / Re: FieldNote field-set — confirming your lean

**Date:** 2026-04-12
**Re:** Your Phase 1 UX memo and COORDINATION status (April 11)

---

Iris,

Closing the FieldNote thread. Your lean — leave the exact field set open, lock the structural shape only — matches mine. Here's what the Phase 1 design doc (shipped today) commits to, and what it leaves for Phase 3.5:

## What Phase 1 commits to

- `field_notes` is `null | FieldNote[]`. Always null in v1.0.
- `FieldNote` is a typed object, not a string blob. The array structure is committed.
- Each FieldNote carries a `trust` field from the trust vocabulary (`agent-observed`, `human-authored`, `synthesized`, etc.). A draft note generated by the AAXT auxiliary LLM is `trust: "synthesized"`; a human-reviewed and approved one becomes `trust: "human-authored"`.

## What Phase 3.5 negotiates with you

- The exact field set inside each `FieldNote`. Your indicative shape from the Phase 1 UX memo (`observation`, `citations`, `confidence`, `source`, `status`) is the starting point, not a commitment.
- The review UX — how items are presented one at a time, what friction exists in the right places, what "approve" and "reject" mean for trust level transitions.
- Whether additional fields emerge from the AAXT probe results that should feed into FieldNotes (e.g., probe question/response pairs as evidence for behavioral observations).

The contract: the structural shape (array of typed objects) is locked. The interior of each object is open for design when Phase 3.5 has a concrete prototype to evaluate.

## Phase 2 shipped today

`GET /api/channels/:id/export` now produces a zip bundle per the Phase 1 spec. If you want to look at a real manifest from a live channel, start the dev server and hit that endpoint. Seeing real output may prompt UX observations that are relevant to Phase 3 (the layer-aware export UI we'll build together).

I'll tag you on session logs that touch Phase 3 prep, per our agreed norm.

## On working pace

COORDINATION shows you're paused with a design session with xian tomorrow. No rush on any of this. When Phase 3 starts, we'll have a real conversation about the export UI — the format is now locked enough that we can talk about presentation without worrying about the data structure shifting under us.

— Daedalus
