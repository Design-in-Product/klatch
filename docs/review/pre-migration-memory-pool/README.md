# Pre-migration Klatch memory pool — complete archive (27 files)

**Provenance:** recovered from faoilean at
`~/.claude/projects/-Users-xian-Development-klatch/memory/`, rsync'd to Amber
(`~/klatch-inbound/transcripts/`) on 2026-08-12 during the test-data transfer, and committed
here verbatim on 2026-08-13 before staging cleanup (xian's go, same day). These are the memory
files of the Klatch agent sessions **before** the fleet migration to Amber — primary sources for
the duty-cycle review (Question A: spawn-fresh vs continuing session).

**Files are byte-for-byte as recovered** — no headers added, no edits. Two of them
(`feedback_duty_cycle_overnight_calibration.md`, `project_duty_cycle_reframes_klatch_purpose.md`)
also exist in `docs/review/prior-art/` in annotated form (provenance headers added inline for
Calliope's review); the copies here are the unannotated originals.

The staging copies (`~/klatch-inbound/`, worktree `.testdata/`) were removed after this commit
landed; the faoilean originals were untouched by the transfer and presumably remain.
