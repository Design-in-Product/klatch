# 2026-09-16 — Calliope (Sonnet 5) session log

## SWEEP fire, ~17:01 PT

`git pull origin main --ff-only`: already up to date. `git log --oneline 5b69b315..HEAD` (my own 9/16 MID-fire checkpoint, rollup v133) showed eight new commits, none mine: Round 220 (Daedalus, `21db36a0`/`98473e91`/`26116a80`), Round 221 (Theseus, `dbc5f7f1`), Argus's own WORK-fire sweep of Rounds 218/219/220 (`5fba0d0d`), and each agent's mail/log/coordination commits.

Read both new mail files in full: `daedalus-to-theseus-cc-xian-janus-argus-calliope-iris-your-6a-had-a-third-site-and-the-corpus-item-is-closed-2026-09-16.md` and `theseus-to-daedalus-cc-xian-janus-argus-calliope-iris-your-hoist-broke-my-probe-loudly-and-then-a-probe-of-mine-graded-a-strangers-process-2026-09-16.md`. Neither addressed to Calliope by name; both cc-only, no reply owed.

**Round 220 (Daedalus):** grepped the whole tree for the size-cap literal rather than trusting only the two files Theseus's §6(a) named, and found a third: `MessageInput.tsx:117` held both the 10 MB threshold and its sentence as bare literals. Unlike the two server-side sites (where a disagreeing sentence is just a confusing error message), a disagreeing threshold *here* is worse — raise the server cap and the attach button keeps refusing at the old number, and since the request never gets sent, nothing about the mismatch is ever logged anywhere. Fixed by moving `MAX_FILE_SIZE_BYTES`/`formatFileSizeLimit()` to `@klatch/shared`; all three sites (`MessageInput.tsx`, `files.ts`, `storage.ts`) now derive from the one constant, `storage.ts` re-exporting it so six tests mocking `../files/storage.js` keep resolving.

Two more instances of the same defect class, caught while building the fix rather than after: (a) `formatFileSizeLimit`'s first draft defaulted its argument to `MAX_FILE_SIZE_BYTES` — a default parameter binds the *declaring* module's constant, which `vi.mock` doesn't reach, so under a mocked cap the helper answered `10 MB` by default while every real call site answered the mocked value — the bug being fixed, reintroduced one layer down inside its own fix. The default is gone; the cap is now a required argument at both helpers. (b) the test's own mocked cap (2.5 MB) was too weak: `files.ts`'s old raw-division formatting and the new shared formatter print the identical string for any cap with at most one decimal, so a mutation reverting `files.ts` to its own arithmetic stayed green — the exact "two sentences agree by coincidence" shape Theseus's §6(a) found, one decimal further out. Re-aimed to 2.25 MB, where the formatter rounds to `2.3 MB` and raw division prints `2.25 MB` — only then did the mutation redden.

Separately, `probe-round220-reassign-on-the-march-corpus.mts` drove Round 212's reassign endpoint against xian's real March corpus for the first time (139 channels, 68 entities, 138 of 139 channels seated on default-entity alone) — 32/32 checks, 0 failed, no defect. The heaviest real seat (174 stamped messages) moved whole, `added_at` preserved; blast radius verified cell-by-cell against a pre-image (exactly 174 `entity_id` cells changed, none outside the channel) rather than merely counted. Two tautologies in the probe itself caught by reading output, not mutation — one compared a subquery against itself and would have read "ok" on a run that had just emptied all 139 channels; rewritten against a real baseline and confirmed to catch the injected defect. One limit measured, not a bug: `fromEntityOrphaned` is hardcoded false for `DEFAULT_ENTITY_ID` (`queries.ts:651`), so the orphan report is structurally silent on 137 of this corpus's 138 possible reassigns — documented behavior, worth knowing the one corpus available can't exercise it (a non-default source shows the flag does work).

§5: confirmed both DELETE-route code sites again (`entities.ts:228-230`, `queries.ts:483`) and split what's routed to xian into two separable shapes rather than one abstract question: (1) the floor question itself — refuse the delete like the sibling route, or treat empty channels as legal and call the *other* route's floor the inconsistency — and (2) independent of that call, `DELETE /entities/:id` returns 200 with no mention of which channels it just emptied, which is wrong either way (1) goes and needs no premise answered. Updated the rollup's needs-you item with this split.

**Round 221 (Theseus):** took Daedalus's own offer ("your probe should stay green, and if it doesn't, that is the news") and drove it. Round 219's probe crashed at zero arms run — it read `MAX_FILE_SIZE_BYTES` from `files/storage.ts`'s old location, which Round 220's hoist moved. Repaired to read `packages/shared/src/types.ts` directly (deliberately not by tree-searching the identifier, since six files still carry the old literal in a mock). Re-driven: 28/28, 15 measurements, 1 open — §6(a) reconfirmed at the wire via two live sentences produced by two different functions, not source text.

Then, running the Round 217 probe cold, **it reported `22/22` on a run where its own server never bound the port and never answered a single request.** Chain: an earlier run piped to `head` left a zombie server holding port 3001 (the pipe closing sent SIGPIPE, which skipped the shutdown handler); the next probe's own pre-flight (`net.createServer().listen()`, "is the port free?") reported free anyway, because macOS/BSD's `SO_REUSEADDR` permits a specific-address bind alongside an existing wildcard-address one; the probe proceeded, and the zombie answered every request in the real server's place, printing `PASS` for every arm, until the first arm that read the (nonexistent, never-created) scratch database threw `SqliteError`. This is a fourth face of the week's recurring pattern (source-text-as-behavior-proxy, self-derived test inputs, a check whose two sides are the same expression): a guard that tests a different property than the one it's named for — and the nastiest of the four, since it sits below every check, so everything downstream inherits the error while looking impeccable.

The hygiene check specifically written to catch this — "xian's klatch.db was never the target" — was itself vacuous: it read `process.env.KLATCH_DB`, the *probe's own* environment, which is always undefined there (the value is only passed to the spawned child); it passed on the exact run it existed to catch. Repaired with two sides from different places: a fixture id fetched over live HTTP, looked up as a row in the file actually handed to the child as `KLATCH_DB`. New `probe-round221-probe-ownership-control.mts`, 9/9: a stub occupant on the wildcard address, both the old bind-test (reports free) and new request-test (finds the occupant) run against it, then both live probes confirmed to refuse starting against an occupied port — exit code 2, and separately, zero `PASS`/`FAIL` lines printed at all, which Theseus flags as the check he'd keep if he could keep only one (an exit code says the guard fired; the absence of any grading says nothing was graded, which is the actual harm). Both probes now reap their child on `SIGINT`/`SIGTERM`/`SIGHUP`/`SIGPIPE`/`exit`, closing the leak that started the whole chain. The earlier `22/22` from this same fire is explicitly withdrawn — it graded a stranger's process.

**Verified myself, not trusted:** re-ran both suites fresh — server **1884/1885 (119 files, 1 skipped)**, client **324/337 (13 skipped, 38 files)** — matches Daedalus's Round 220 count exactly, and matches Argus's independent WORK-fire sweep of the same rounds. `npm run typecheck` clean across all three workspaces. Read the code directly rather than take either memo's word: `packages/shared/src/types.ts:205,230` (the constant, and the formatter's now-required argument, no default), `storage.ts:4,14` (import + re-export), `files.ts:23,25,71,73` (pre-read cap using the shared formatter), `MessageInput.tsx:2-3,138-139` (imports the shared constant/formatter, no cap literal left — the `1024 * 1024` at :148-149 is display formatting only), and `ProjectSettings.tsx` (confirmed no client-side size gate exists there at all, matching both memos' claim). Spot-checked the Round 221 probe and Round 217's SIGPIPE-reaping fix structurally (`grep` for the signal list and the check functions) rather than executing either live-socket probe myself this fire — that instrument class belongs to Theseus/Argus, and Round 221's own finding is itself a caution against trusting a probe's printed count without independently confirming what it ran against, which this seat isn't set up to do safely mid-chronicling-pass.

**Rollup refreshed to v134** (`docs/operations/attention-rollup.md`):
- New top banner covering both rounds in full, v133 preserved verbatim under "Prior banner (v133, superseded)".
- Needs-you section: the entity-delete item retitled and rewritten to carry Daedalus's §5 two-shape split rather than stand as one abstract question.
- Metrics strip: new footnote, count unchanged at 3, explaining the sharpening; prior footnote pushed to "superseded."
- Changelog: new `v134` entry.

Standing blockers re-checked, both unchanged: Janus's logbook-shape thread (parked on xian since 8/28, now 19 days, still unanswered); the HTML mirror (`docs/operations/attention-rollup.html`, flagged 9/7, still last-built 8/23, source now further ahead than any prior check — day 9 of the same named gap, not attempted again this fire for the reasons given in the MID-fire log). `ls docs/mail | grep '^xian-to'` empty — no new mail from xian.

No `packages/` changes this fire (chronicling only). Committing `docs/operations/attention-rollup.md`, `docs/COORDINATION.md`, this log.
