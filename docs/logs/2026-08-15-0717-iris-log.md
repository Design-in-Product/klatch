# 2026-08-15 START fire (~07:17 PT) — Iris

## Session-start protocol

- `git pull origin main` — already up to date, `HEAD` at `b309110`.
- Read `docs/COORDINATION.md` (my section, `docs/COORDINATION.md:304-375`) and `docs/ROSTER.md` (my
  entry, `docs/ROSTER.md:114-120`) to reconfirm role scope before touching mail.
- `docs/mail/` sweep. Not addressed/cc'd to me and already actioned per my 8/14 STOP-fire log entry:
  pre-tool narration display, `save_file` card question (both from `neighbourhood-landed` /
  `recall-tool-landed`) — verified this by checking git blame on `ecf259e` (my 8/14 STOP-fire commit),
  not by trusting the earlier log entry's own claim.
- Two items genuinely still open, across a four-memo chain (round49 → recall-tool-landed →
  recall-probe → neighbourhood-landed → round51): the `updateMessage` array-replace flag (Theseus,
  flagged twice) and the recall `tool_use` card-weight question (Daedalus).

## Work this fire

**1. Fixed the array-replace flag.** `App.tsx`'s `handleStreamComplete` synthesized the
carried-context artifact as `artifacts: [chip]`, which replaces rather than merges with whatever the
optimistic message already held — safe today only because that array starts empty, per Theseus's own
read. Changed `useMessages.ts`'s `updateMessage` to accept an updater-function form
(`(m: Message) => Partial<Message>`) alongside the existing plain-object form, and switched
`handleStreamComplete` to filter-and-append:
`artifacts: [...(m.artifacts ?? []).filter(a => a.type !== 'carried_context'), chip]`. Added
`packages/client/src/__tests__/useMessages.test.ts` (3 tests): plain-object form unchanged, updater
form reads current-message state to merge not clobber, and re-running the updater on retry replaces
its own prior entry rather than duplicating it.

**2. Decided the recall card-weight question.** Read `ToolCards` and the carried-context row
side-by-side in `MessageList.tsx` before deciding, rather than reasoning from the "card" name alone —
they're already the same `text-xs text-muted` single-line weight; the real difference is content
(query string vs. bare count), which is the correct difference given the chip's existence-only rule
exists specifically to keep carried *content* off the screen, and a recall query isn't carried content.
Decision: no change to `ToolCards` for recall. Flagged (not decided) the live/reload asymmetry
Theseus measured — recall's `tool_use` artifacts don't ride the wire the way `carriedContext` now
does — as Daedalus's call on whether it's worth the same wire-field treatment.

**3. Mail hygiene.** Filed `iris-to-daedalus-cc-theseus-team-round49-flag-fixed-and-card-weight-decided-2026-08-15.md`
cc team. Closed the now-fully-resolved 8/13 reload-time-gap thread to `docs/mail/read/`
(`theseus-to-iris-cc-daedalus-team-the-chip-is-correct-and-absent-when-it-matters-2026-08-13.md` +
`iris-to-theseus-cc-daedalus-team-reload-time-gap-decided-2026-08-14.md`) — Theseus's round49 memo
verified the carried-context wire field live from his own execution, so nothing was left open on
either side. The round49/recall-tool-landed/recall-probe/neighbourhood-landed/round51 chain itself
stays open (Daedalus's tool_use wire-field call is still outstanding) — only my two named items close.

## Verification

- `npm run typecheck` — clean ×3 workspaces (shared, server, client).
- `npm test` (root) — 1319 server (unchanged), 230 client (+3), exit 0.
- `npm run build` — green end-to-end, full `vite build` of `packages/client/dist`.
- Both standing blockers (import-confirm-step-ux, ground-rules-UX) re-checked via `git log --follow` /
  `git log --all --grep` — unmoved since 8/14, still genuinely parked, not stalled on me.

## Commits

Verification of what actually landed follows in the wrap section below, per the Session Wrap Protocol
(commits happen after this log entry is written, not before — this entry is drafted alongside the
work, not reconstructed from memory at the end).
