# Iris — 2026-09-14 START fire (~07:17 PT)

No-op, verified not assumed.

- Pulled clean to `d3bfb2c5` (Calliope's 9/14 cross-poll brief commit); worktree already synced at fire start.
- Mail sweep since my own 9/13 STOP checkpoint (`59740556`): one new file (`git log --oneline --diff-filter=A --name-only 59740556..HEAD -- docs/mail/`) — `theseus-to-daedalus-cc-xian-janus-argus-calliope-204-holds-and-there-are-two-resolvers-not-one-2026-09-13.md`. Checked directly with `grep -li iris`: zero hits, not even a cc mention. Backfill-CLI research track (plan/apply resolver divergence), outside Iris's UX lane — same item the 9/14 cross-poll brief covers as its headline insight.
- Cross-pollination brief (`docs/briefs/cross-pollination/current.md`, dated 2026-09-14) read in full: plan/apply id-vs-label divergence (Klatch Rounds 204–205) and shared-test-vehicle layering lesson (One Job). Both process/data-integrity lessons from the backfill-CLI arc, no UX-lane action.
- Standing blockers re-checked directly, not recalled: `ls docs/mail | grep "^xian-to"` empty (ground-rules-UX still blocked on Calliope's open question to xian); `grep -rl entityGuess packages/client/src` same 5 files as prior fires (`ImportDialog.tsx`, `client.ts`, `ImportDialog.test.tsx`, `round173-import-identity-on-every-route.test.tsx`, `round174-browse-done-seating.test.tsx`) — an initial parallel-tool-call run returned empty, re-ran serially and got the expected 5; treating the empty result as a transient fluke, not a signal, since the re-run reproduced the standing count exactly. Queued `entityId` import-dialog item unmoved.
- `packages/` diff since the 9/13 STOP checkpoint (`59740556..HEAD`): empty (`git diff --stat`) — the one intervening commit is mail-only.
- Verified, not trusted: server **1683/1683 (104 files)** — unchanged from Argus's 9/13 STOP numbers; client **311/311 (13 skipped, 37 files)** — unchanged; `npm run typecheck` clean ×3 workspaces (via `npm test` at root).

No `packages/` changes this fire.

---

# Iris — 2026-09-14 STOP fire (~19:2x PT)

Real product work: the `entityId`/import-picker item unblocked this morning by Daedalus's Round 208 server contract, built the safe half.

- Pulled clean at `acfee0b1` (Argus's own 9/14 STOP sweep commit).
- Mail sweep since my own 07:17 checkpoint (`d3bfb2c5`): eleven new files (`git log --since="2026-09-14 07:17:00" --diff-filter=A --name-only -- docs/mail/`). Four cc me directly (checked each with `grep -n -i iris`): Daedalus's Round 210 retire-arms memo (mentions the picker is still my unbuilt work — informational, no new ask); Theseus's Round 209 reply (same, "picker is still unbuilt client work"); Daedalus's Round 208 coin-flip correction (cc-only mention); and the one with an actual routed item — `daedalus-to-theseus-cc-...-your-picker-is-server-complete-and-the-tiebreak-is-worse-than-206-said-2026-09-14.md` (WORK fire, `f1aebb05`): `ImportResponse.sameNameEntityIds` now present exactly when a confirmed import name matched more than one entity — "Iris: that's the whole contract... happy to build it if you'd rather I did." The other seven (janus↔calliope §7.2/§4c ratification, calliope↔daedalus Cowork-merge correction) are outside Iris's UX lane, Iris not mentioned.
- **Read the full Round 208 memo, not just the "Iris:" line**, before deciding what to build — needed items 1-4 for context on what the field means and how it's populated.
- **Checked the actual mechanism before designing a fix, not just the field's presence.** `entity-resolve.ts:133-148` confirmed `sameNameEntityIds` is set only on the `matched-by-name` disposition, listing every entity tied on the confirmed name — the bind itself already took `matches[0]` under `(created_at ASC, id ASC)`, which Round 208 independently found degenerates to a coin flip under a same-millisecond tie. `importSession` (`db/queries.ts:1289-1346`) confirmed assistant messages get `entity_id` stamped at insert time, separately from the `channel_entities` join row inserted in the same transaction — a fact the original 8/09 scope doc's §2 picker design predates and doesn't account for.
- **Decision: build the disclosure, not a reassign action, and say why.** §2's picker was designed as a *pre-import* alternative to free text; the actual server signal is *post-import* — the bind already happened, `sameNameEntityIds` says it was arbitrary. Reusing `assignEntityToChannel`/`removeEntityFromChannel` (the only client-callable primitives) to "fix" the bind would touch `channel_entities` only, leaving every message's `entity_id` on the old entity — the identical defect class (stale `messages.entity_id` surviving a rebind) the Daedalus/Theseus/Argus backfill-CLI arc (Rounds 200-210) spent this week finding and pinning tests against on the *other* rebinding path. Declined to ship a narrower version of the same gap from the client; routed the "does this need a server endpoint" question back to Daedalus instead of guessing.
- **Built:** single-import success panel (`ImportDialog.tsx`, `result` state) gains the mint-vs-merge disposition copy the 8/09 scope doc specified — checked the 9/2 build note first and confirmed this panel never actually got it, only the multi-import list did — plus the new ambiguity line. Bulk/Browse result rows get the same ambiguity line per-row. `ClaudeAiImportResponse.imported[]` (`client.ts`) and the local array type in `handleImportSelected` both gained `sameNameEntityIds?: string[]`, threaded from `result.data.sameNameEntityIds`.
- New tests: 3 single-import-panel tests (minted copy, ambiguity note present, ambiguity note absent when name is unique) + 1 bulk-row test (note on the ambiguous row only, not its sibling) in `ImportDialog.test.tsx`.
- Doc: `docs/ux/import-confirm-step-scope-2026-08-09.md`, new 2026-09-14 section — re-scopes §2 rather than silently reinterpreting it. Mail filed: `iris-to-daedalus-cc-theseus-team-samenames-disclosure-built-reassign-needs-a-server-endpoint-2026-09-14.md` — thread stays open, the server-endpoint question is the outstanding action, correctly Daedalus's.
- Standing blockers re-checked directly: ground-rules-UX still blocked on Calliope's open question to xian (`ls docs/mail | grep "^xian-to"` empty).
- **Verified, not trusted:** `npm run typecheck` clean ×3 workspaces; server **1785/1785 (112 files)**, unchanged (no server files touched, confirmed via `git diff --stat -- packages/server`); client **315/315 (13 skipped)**, +4 from this morning's 311, exactly the four new tests added; `npm run build` green end-to-end (`vite build` included, not just typecheck).

