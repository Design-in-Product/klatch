# Iris — 2026-08-14 (STOP fire, ~19:20 PT)

## Session-start protocol, run in full

- `git pull origin main` — already up to date (worktree synced by the wrapper immediately before this
  fire). `git status` clean at start.
- Read `docs/COORDINATION.md` in full (`### Iris` section, lines 293–362 as of this fire's start), plus
  Argus's and Daedalus's sections above it for same-day context (Round 51 verification, Round 50/51
  landings).
- Swept `docs/mail/` (`ls`, then read every file mentioning Iris landed since the 07:17 fire). Three
  live items, all new this fire:
  1. `daedalus-to-iris-theseus-cc-team-room-count-and-wire-field-landed-2026-08-14.md` — `StreamEvent.
     carriedContext` landed server-side, my client half unblocked.
  2. `daedalus-to-theseus-cc-iris-xian-team-neighbourhood-landed-option2-is-yours-to-rule-2026-08-14.md`
     §6 — pre-tool-narration display, routed, not blocking.
  3. `daedalus-to-theseus-iris-cc-team-recall-tool-landed-2026-08-14.md`, "Iris" section — `save_file`
     tool-use card question, routed, not blocking.
  Everything else addressed to Iris was either my own outbound mail or a standing blocker re-checked
  below, unchanged since the morning fire.

## Work this fire: built the client half that closes the reload-time gap end to end

Item (1) was exactly the shape my own 8/14 START-fire decision doc specified, and Daedalus's memo
confirmed the server side landed to that spec (verified against code, not the memo's prose alone —
read `StreamEvent` in `packages/shared/src/types.ts:370-399` directly, confirmed `carriedContext` is
optional, the formatted `inputSummary` string only, absent not empty-string).

**`useStreams.ts`:** `onComplete`'s signature gained a fourth optional arg, `carriedContext`, threaded
from `data.carriedContext` on the `message_complete` branch — mirrors the existing `stopReason`
passthrough exactly (same file, same pattern).

**`App.tsx`:** `handleStreamComplete` now accepts the fourth arg and, when present, merges a
synthesized one-element `MessageArtifact[]` into the `updateMessage` call: `{ id:
'${messageId}-carried-context-live', messageId, type: 'carried_context', inputSummary:
carriedContext, createdAt: new Date().toISOString() }`. Deliberately the same shape `fetchMessages`
would return from the DB on reload — `ArtifactList` (`MessageList.tsx:95-140`) needed zero changes,
since it already renders whatever `message.artifacts` it's given via `.find((a) => a.type ===
'carried_context')`. One render path now serves both the live-turn chip and the reload chip, which
was the actual point: they can no longer drift apart the way this gap started (Theseus's 8/13 finding
was exactly that drift).

Checked before writing: the optimistic assistant message created in `handleSend`/`handleSendWithFile`/
`handleRegenerate` (`App.tsx:134-278`, three call sites, all feeding the same `useStreams` instance)
never sets `artifacts` — so there's no existing array to merge against; a bare assign is correct, not
a partial-merge bug waiting to happen.

## Tests

Extended `useStreams.test.ts`'s two existing `onComplete` assertions (`toHaveBeenCalledWith(...)`) to
include the new fourth arg — both were failing on arity before the fix (confirmed by running them red
first, not assumed), now pass with an explicit `undefined` for the no-context case. Added one new test:
`carriedContext` passthrough when `message_complete` carries the field. Did not add a new App-level
integration test — same named gap as the 8/11/8/12 `stopReason` work (no App+streams harness exists
cheaply extendable), and the render-layer coverage for the artifact shape already exists in
`round48-carried-context-chip.test.tsx`, so the two already-tested layers (hook passthrough, render
branch) plus this new passthrough test cover the seam without inventing a third harness for a
one-line merge.

**Verified:** `npm test` — **1319 server (unchanged) / 227 client (+1)**, exit 0. `npm run typecheck`
clean ×3 workspaces. `npm run build` green end-to-end, including a full `vite build` of the client
`dist/` (not just typecheck) — confirmed no bundler-level regression from the shared-type change.

## Two more routed decisions, no code

Items (2) and (3) above, both "yours, not blocking me" per Daedalus. Decided both, recorded reasoning
in `docs/ux/carried-context-visibility-2026-08-13.md`'s new 8/14 STOP section rather than only in mail
(deliverables rule):

- **Pre-tool narration stays plain prose.** No client-side signal distinguishes a round-boundary
  `\n\n` in `message.content` from an ordinary paragraph break without new wire-level plumbing (same
  family as `stopReason`/`carriedContext`); styling on the wrong signal would sometimes mis-style a
  normal two-paragraph answer. Daedalus's own framing wasn't a reported confusion — not building ahead
  of demonstrated need, same bar as the 8/13 `hasOlderHistory` call.
- **`save_file` does not get a `tool_use` card.** Not symmetric with `search_my_other_conversations`:
  that tool has no other surface, so its card is the only signal it ran. `save_file` already renders
  `FileCard` — strictly more informative than a tool row would be. A second card duplicates signal
  rather than adding it, on top of Theseus's measured 2.2 cards/turn. Resolves both of Daedalus's
  sub-questions (no row; the weight question is moot with no row to weigh).

## Mail hygiene

- Reply filed: `iris-to-daedalus-cc-theseus-team-wire-field-built-and-two-decisions-2026-08-14.md`
  (cc Theseus, xian, Argus, Calliope, Pard).
- **Moved to `read/`:** `daedalus-to-iris-theseus-cc-team-room-count-and-wire-field-landed-2026-08-14.md`
  and the reply above — the wire-field item (mine) and the room-count item (Theseus's, already
  confirmed landed in the memo's own text, no open action stated) are both closed; nothing outstanding
  on this thread.
- **Not moved:** the neighbourhood-landed and recall-tool-landed memos — both carry open items that
  aren't mine (option (2) is explicitly routed to xian; the live-call probes are Theseus's). Only the
  Iris-addressed sub-items are closed by this fire's reply; the parent threads stay open.
- Re-checked both standing blockers rather than carrying forward the morning entry unverified:
  `git log --follow -- docs/ux/import-confirm-step-scope-2026-08-09.md` — still only the 8/09 authoring
  commit, no xian follow-up. `git log --all -i --grep="ground-rules\|blank-slate"` — same single hit as
  every prior check this cycle. Both still genuinely parked, not stalled on me.

## Verify-before-asserting checks run this fire

- `StreamEvent.carriedContext`'s exact shape, `ArtifactList`'s render condition, `MessageArtifact`'s
  required fields, and all three `addMessage` call sites in `App.tsx` — read directly from source this
  fire, not recalled from Daedalus's memo prose or the morning's decision doc.
- Ran the two edited `useStreams.test.ts` assertions red (arity mismatch) before fixing them, rather
  than assuming the fix was needed from the diff alone.
- Test/typecheck/build counts above are this fire's own run output, not carried forward from
  Daedalus's memo.

## Git verification (session wrap protocol) — appended after push

