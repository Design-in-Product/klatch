# Session Log — Argus (Opus 4.6)
**Date:** 2026-03-21, 22:56 UTC
**Branch:** `claude/audit-and-planning-xn2w7`

---

## 22:56 — Session start

Pulled from origin, rebased onto main (resolved one conflict in intel sweep file). Read all mail:

- **Calliope demo infra spec (Mar 19):** Seed script overhaul, DEMO.md, Playwright recording
- **Calliope reliability incident (Mar 20):** Previous session lost demo work in bad rebase + force push. Logged completion of work that wasn't in repo. Trust failure acknowledged.
- **Theseus AAXT harness (Mar 20):** Round 11 — automated AX testing via prompt-debug endpoint
- **Daedalus Round 9 (Mar 18):** Settings polish tests + cloud env research
- **Daedalus Round 10 (Mar 19):** Cloud import test coverage

**Tonight's priorities:**
1. Demo infrastructure redo (trust repair — verify every file before claiming done)
2. Round 11 AAXT harness
3. Anything Daedalus sends

## 23:00 — Audit of current state

Confirmed what's missing vs. what exists:
- `scripts/seed-demo.sh` — exists but points to `klatch.db`, no project creation, old entity names (actually updated names from v0.6.0 era)
- `scripts/demo-seed.sh` — older, simpler version (code-reviewer + brainstormer channels)
- `docs/DEMO.md` — does NOT exist (only `docs/DEMO-PLAN.md` from v0.6.0)
- `KLATCH_DB` env var — NOT supported in server (DB path hardcoded in `db/index.ts`)
- `scripts/record-demo.ts` — does NOT exist

Starting with KLATCH_DB env var support, then seed script, then DEMO.md.

## 23:05 — Demo infrastructure rebuild

### KLATCH_DB env var support
Added `process.env.KLATCH_DB` support to `packages/server/src/db/index.ts`. If set, uses `path.resolve(KLATCH_DB)` as the database path; otherwise defaults to `klatch.db` at project root.

### seed-demo.sh overhaul
Rewrote `scripts/seed-demo.sh`:
- No longer starts/stops the server (expects it to be running)
- Creates "Mystery Menu Restaurant" project via `POST /api/projects`
- Creates channel with `type: 'klatch'`, `mode: 'roundtable'`
- Assigns channel to project via `PATCH /api/channels/:id`
- Creates Chef Margaux, Sam (FOH), Julien (sommelier)
- Assigns all three to channel, removes default entity
- Updated usage instructions to reference `KLATCH_DB=demo.db`

### docs/DEMO.md
New file documenting the full demo workflow: prerequisites, quick start, what the seed script creates, the demo prompt, database isolation convention, and recording instructions.

### scripts/record-demo.ts
Playwright recording script with human-like typing simulation (variable delays, occasional backspace corrections). Records to `web/assets/`. Requires Playwright installation (not yet a project dependency).

## 23:25 — Round 11 AAXT harness

Built `packages/server/src/__tests__/round11-aaxt-harness.test.ts` — 21 tests across 5 describe blocks:

**Group A — Claude Code import (5 tests):**
- A1: Basic import, no project — kit briefing fires, layers 2-3 empty
- A2: Import with claudeMd (no project) — appears in kit briefing legacy fallback
- A3: Import with memoryMd (no project) — appears in kit briefing legacy fallback
- A4: Import linked to project — layers 2-3 populated from project
- A4b: Legacy fallback suppressed when project is linked

**Group B — Cloud session import (4 tests):**
- B5: Cloud upload — kit briefing fires, cloudUpload flag set
- B6: Basename project matching — links to existing project
- B6b/B6c: findUniqueProjectByName — exact match and zero match
- B7: Ambiguous basename — returns undefined

**Group C — claude.ai ZIP import (3 tests):**
- C8: prompt_template → layer 2 (project instructions)
- C9: Merged memories → layer 3 (project memory), no character-array bug
- C10: No project_uuid match → unassigned, layers 2-3 empty, claude.ai in briefing

**Group D — Edge cases (4 tests):**
- D11: Re-import creates separate channel with same prompt assembly
- D12: Empty instructions → layer 2 EMPTY, no blank block injected
- D12b: Empty memory → layer 3 EMPTY
- D13: Native channel — no kit briefing at all

**buildSystemPrompt unit tests (5 tests):**
- Layer ordering verification (kit < instructions < memory < addendum < entity)
- Native channel: no kit briefing
- Legacy fallback fires without project
- Legacy fallback suppressed with project

All 21 tests pass. Full suite: 589 server + 116 client + 21 AAXT = 726 total, zero failures.

## 23:35 — Session wrap verification

Ran verification protocol per Calliope's reliability memo. All 6 deliverables confirmed present:
1. `KLATCH_DB` env var in `packages/server/src/db/index.ts` — ✓
2. `scripts/seed-demo.sh` with demo.db + project creation — ✓
3. `docs/DEMO.md` (74 lines) — ✓
4. `scripts/record-demo.ts` (154 lines) — ✓
5. `round11-aaxt-harness.test.ts` (758 lines, 21 tests) — ✓
6. This session log — ✓

**Remaining from previous assignments (not done this session):**
- Rounds 9, 10 test suites — noted, lower priority tonight
- Playwright dependency installation — deferred (script is ready but needs `npm install -D playwright`)
