# Calliope session log — 2026-09-12

## START fire, ~08:32 PT

`git log --oneline f9a92295..HEAD` (my own 9/11 STOP checkpoint, rollup v124) showed exactly two new commits, neither mine: the 9/12 cross-pollination brief (`337ba7ef`, Piper Morgan's DAY-CLOSED self-heal-circularity finding — a monitoring lesson, no Klatch product action) and Iris's own 9/12 START no-op (`8837e5d1`).

`git diff --stat f9a92295..HEAD -- packages/` empty. `git diff --stat f9a92295..HEAD -- docs/mail/ docs/COORDINATION.md docs/ROADMAP.md docs/operations/` shows only Iris's 2-line COORDINATION entry — no rollup edit, no mail added or moved.

Mail sweep: `ls docs/mail | grep -i to-calliope` / `cc-calliope` returns the same set of standing threads as prior fires (Janus logbook-shape 8/28, Daedalus backfill-sizing, the 9/3–9/4 Browse-count/cap-cost/turncount/import-size threads, Cowork import-defects 8/28) — all already closed-out or already tracked as blocked-on-xian in earlier rollup entries, none new. `ls docs/mail | grep '^xian-to'` empty.

**Verified, not trusted:** re-ran the full suite myself — server **1615/1615 (101 files)**, client **311/311 (13 skipped, 37 files)**, both exactly matching the 9/11 STOP numbers; `npm test` (typecheck → server → client) completed clean end to end. Rollup re-checked directly against v124 — still accurate, nothing landed that it doesn't already state.

No product action this fire. Coordination entry appended, no-op.

## MID fire, ~12:31 PT

`git log --oneline de8987dc..HEAD` (my own 9/12 START checkpoint) showed eight new commits, none mine: Argus's 9/12 START sweep (Round 193 independently re-verified, no `packages/` change), then two rounds on the restore/corruption research track — Round 194 (Daedalus, `f29edea6`+mail+`0fe71b6a`) and Round 195 (Theseus, `174a19d0`+mail+`4e20819c`).

Read both memos and both research writeups in full, not summarized from mail alone. **Round 194 (Daedalus):** step 4 now quotes the exact `Candidates:` line the operator's own no-flag re-run will produce, via a new `candidatesLine(plan)` shared with the printed line — reasoned per case (plain run / `--channels`-or-`--bases`-filtered / post-apply for `--undo`, computed before `undoEntityBackfill` writes). Adopted Theseus's Round 193 framing outright: "1,500 messages" was an artifact of the pre-192 checkpoint gap, not a property of the corruption arm — true width is one write, corrected in three places. Five negative controls, each failing only what predicted. Server 1615→1621 (+6), verified myself, not taken on word. **Round 195 (Theseus)** reproduced Round 194 cleanly (14·0·0, unmodified) then drove the branch Round 194 didn't reach: the operator whose restore *failed*. All three of the tool's own next-step commands answer a corrupted database badly — M2 (step 4's own re-run crashes with a raw Node stack trace), M3 (a second `--apply` crashes the same way, leaves an extra corrupt snapshot beside the database), M4 (`--undo` takes a fresh snapshot from a read-only handle that doesn't validate pages, calls it "intact," and points the operator at that same corrupt copy — the real backup is still there and recovers byte-for-byte when used). Also N2: a correct restore's own filtered command, pasted from memory, quotes a different line than step 4 does — a correct restore reported as failure, reached from the operator's side. No `packages/` change this round (one probe, two documents); server suite unchanged at 1621.

**Verified independently, not trusted:** re-ran the suite myself — server **1621/1621 (101 files)**, up from 1615, matching Daedalus's stated count; client **311/311 (13 skipped, 37 files)**, unchanged; `npm run typecheck` clean across all three workspaces. `git fetch origin` clean, HEAD matched `origin/main` at `4e20819c` before this fire touched anything. `docs/ROADMAP.md`'s backfill line re-checked — still accurate. Both new mail files, both writeups, and both probe scripts checked to exist directly with `ls`.

**Rollup refreshed to v125** (`docs/operations/attention-rollup.md`): new "Last refreshed" banner, four (not three) lines for xian — the new fourth line: if a restore goes wrong, don't trust the tool's own next-step commands yet, the good backup is still on disk under a different file. Daedalus's call narrowed to four named shapes, none built. Old v124 banner demoted to one consolidated "Prior banner" paragraph; new v125 changelog entry added above v124's. Metrics-strip needs-you count unchanged at 3 — this is verification/fix work under the existing Backfill 🔴 item, not a new decision.

**Mail hygiene:** Round 194/195 correctly left open in `docs/mail/` — Theseus's Round 195 opens three new findings and four shapes that are explicitly Daedalus's call, and he hasn't replied yet; not mine to close. No new memo addressed to Calliope requiring action beyond the rollup update (`ls docs/mail | grep '^xian-to'` empty); standing memos to this seat (Janus's 8/28 logbook-shape question, the 9/7 rollup-html-mirror-drift question, the Cowork import-defects thread) re-checked directly, all genuinely still open.
