# Calliope — 2026-09-13 STOP fire (~21:31 PT)

`docs/ROADMAP.md`'s Agent-continuity bullet fixed — flagged stale by Argus twice today (WORK and STOP sweeps), doc ownership sits with this seat. No other action needed.

- Pulled clean, already at `e7f65659`. `git log --oneline 30c6540a..HEAD` (my own 17:01 WORK checkpoint) showed nine new commits, none mine: Daedalus's Round 204 (mail + writeup + wrap — undo driven end-to-end over a 9-entity/821-row role-basis apply, holds byte-for-byte both directions), Argus's and Iris's 9/13 STOP no-op sweeps, and Theseus's Round 205 (mail + writeup + wrap) — reproduces Round 204 exactly, then finds a live, unanswered defect: the plan's name-lookup (`entity-backfill.ts:432`, an unordered `Map`, last-row-wins) and the apply pass's lookup (`entity-resolve.ts` via `getAllEntities()`, `ORDER BY created_at ASC`, first-match-wins) pick opposite ends of a duplicated name. Driven through the CLI on two agents named `Daedalus`/`daedalus`: the plan shows the newer entity approved, the apply binds the older one. **The agent the operator approved is not the agent that got the channel**, and the sheet has no way to show the two ids differ.
- **Verified the Round 205 finding directly against the code, not just the memo chain**, before citing it anywhere: `grep -n "new Map(" packages/server/src/db/entity-backfill.ts` → line 432, `const byName = new Map(existing.map((e) => [normalizeName(e.name), e.id]))`, no `ORDER BY` upstream; `packages/server/src/import/entity-resolve.ts` reads `getAllEntities()` (`ORDER BY created_at ASC`) and resolves via `.find()`. Both claims hold as described.
- **`docs/ROADMAP.md` line 277 (Agent continuity) fixed, not just re-checked.** Argus flagged it stale at both the 9/13 WORK sweep (still carrying Round 199's `72 — 7 would move` / `0-for-7` framing after Round 200 moved the number) and again at STOP (three rounds further behind, through Round 204). Rewrote the paragraph's back half to carry the actual arc through Round 205: Round 200's fix took default-basis candidates to `72 — 0 would move` (correctly — `identity-claim` isn't what this corpus offers, not a defect); Round 201 found the corpus's real shape is role claims, 0 of 0 name-claim recall; Round 202 shipped `role-title` as a second basis (`72 — 9 would move`, 9/9 precision); Round 203 corrected recall to 9/12 and reframed the corpus as role lineages; Round 204's undo holds; Round 205's plan/apply divergence is left open, unanswered. Also corrected: the "no data written" claim now covers Rounds 199–205 as a whole, not just Round 199. Verified the cited numbers directly against `docs/mail/read/daedalus-to-theseus-cc-xian-janus-argus-calliope-all-four-shapes-built-and-the-plan-was-only-advising-the-apply-2026-09-13.md` (Round 202) rather than trusting Argus's summary alone — `Candidates: 72 — 0 would move` / `72 — 9 would move, 821 P2` / `nine of nine names are right` all match verbatim. Diff is `docs/ROADMAP.md` only, one paragraph.
- **Rollup (`docs/operations/attention-rollup.md`, still v127, mtime Sep 12 21:42) not refreshed this fire.** Round 205 opened a fresh, unanswered finding — Daedalus hasn't replied yet — same judgment applied at 13:15/17:01: mid-thread is not rollup time. The ROADMAP fix was the overdue, unconditional item; the rollup can wait for the thread to reach a resting point.
- **Verified, not trusted:** re-ran both suites myself — server **1683/1683 (104 files)**, client **311/311 (13 skipped, 37 files)** — both matching Theseus's and Argus's stated counts exactly, unchanged since Round 204 (Round 205 is scripts/docs-only).
- Standing blockers re-checked directly, all still open, unchanged: Janus's logbook-shape thread (parked on xian, day 17+ since 8/28), the rollup-html-mirror-drift question (9/7), the Cowork import-defects thread's open §1/§4a. `ls docs/mail | grep '^xian-to'` — empty, nothing new addressed to Calliope requiring action.

End of day-part cycle.
