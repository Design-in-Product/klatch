# Iris — 2026-08-14 (START fire)

## Session-start protocol, run in full

- `git pull origin main` clean, nothing to fast-forward — worktree already synced by the wrapper
  immediately before this fire. `git status` clean at start.
- Read `docs/COORDINATION.md` in full (Iris section: lines 269–336 as of this fire's start; also read the
  surrounding Argus/Daedalus/Calliope sections for context on the 8/13 carried-context thread this fire's
  work continues).
- Swept `docs/mail/` (`ls`, then checked every file mentioning Iris). One live, unactioned item:
  **`theseus-to-iris-cc-daedalus-team-the-chip-is-correct-and-absent-when-it-matters-2026-08-13.md`**
  (19:47 PT, the hour after Round 48's chip shipped) — this fire's real work. Everything else addressed to
  Iris is either an outbound memo I already sent or a still-genuinely-parked thread re-checked below.

## Work this fire: closed the reload-time gap Theseus found in Round 48's chip

Theseus drove the carried-context chip (shipped 8/13 STOP fire) through a live server the same hour and
found it renders on reload but not on the turn it's about — the SSE `message_complete` payload carries no
artifact data, and the client's optimistic-update path never refetches. Verified his three claims against
current code before deciding rather than taking the memo's word: `StreamEvent`
(`packages/shared/src/types.ts:370-381`) is confirmed artifact-free; `handleStreamComplete`
(`App.tsx:103-113`) patches from that payload alone; `fetchMessages` — the only path that populates
`message.artifacts` — runs once per channel mount (`App.tsx:49`), never again. His framing is right: this
inverts my 8/13 duplication ruling for the live turn specifically — on that turn the prose hedge (if any) is
the only signal present, and the structural signal the chip exists to guarantee is exactly the one missing.

**Decision: adopt the `stopReason` precedent, not a refetch.** Read `types.ts:270-292`'s own docstring on
`stopReason` — it rides the SSE event for the identical reason (optimistic update, no refetch). Checked
`createCarriedContextArtifact` (`db/queries.ts:1034-1047`) — it already computes and returns the exact
`inputSummary` string the chip needs, before the stream even starts. One optional field on `message_complete`
carrying that string, present only when the artifact was created, closes the gap with the same boundary the
chip already keeps (pre-formatted string only, never the raw `roomCount`/`messageCount`/`omittedCount`/
`hasOlderHistory` fields). Rejected the alternative on the table (call `refresh()` on every completion) —
full channel refetch, discards optimistic state for every other seat mid-stream in a klatch turn, for one
chip.

**Split the same way `stopReason` itself landed** (Daedalus: shared type + server emit; me: client
threading, sequential fires, not simultaneous): read `client.ts:722-737` and confirmed a real wrinkle to
flag rather than let Daedalus discover mid-diff — `createCarriedContextArtifact` runs in
`streamClaude`/`streamClaudeRoundtable` (`client.ts:785,856`) *before* `streamClaudeCore` is called, but
`message_complete` is emitted from inside `streamClaudeCore` (`client.ts:722-729` plus the abort/error
paths), which doesn't see `carried`. `stopReason` didn't have this problem — same function computes and
emits. Left the resolution (thread a parameter down, or move the emit up) to him.

**My half, explicitly not built this fire** — same sequencing as the incomplete-status feature: decide,
then server, then client. Once the field exists: `useStreams.ts`'s `onComplete` passes it through (mirrors
the existing `stopReason` passthrough, `hooks/useStreams.ts:15,67`); `handleStreamComplete` builds a
one-element `MessageArtifact[]` and includes it in the optimistic `updateMessage` call. `ArtifactList`
already renders whatever `message.artifacts` it's given — no change needed there.

**Declined Theseus's offered AAXT fire** for rendered-page confirmation — his code read plus the measured
absence of any artifact-bearing SSE event is sufficient to act on, and the fix pattern already has a passing
precedent in this codebase (`useStreams.test.ts`'s `stopReason` passthrough test). Flagged worth a live
re-drive once built, same as Round 48 got.

**Room-miscount finding (channel-name-not-id counting, `carried-context.ts:311`) is entirely Daedalus's** —
a counting defect, not a visibility question. No action taken on it.

Doc: `docs/ux/carried-context-visibility-2026-08-13.md`, new 8/14 section appended. Reply filed:
`iris-to-theseus-cc-daedalus-team-reload-time-gap-decided-2026-08-14.md` (cc Daedalus, xian, Argus, Calliope,
Pard).

## Mail hygiene

- **Not moved to `read/`.** Daedalus's server-side implementation is the outstanding action on this thread
  (same close-discipline reasoning as every prior carried-context thread this cycle) — stays open with the
  reply filed alongside it.
- Re-checked the two standing blockers rather than carrying forward the 8/13 entry's claim unverified:
  `git log --follow -- docs/ux/import-confirm-step-scope-2026-08-09.md` — still only my own 8/09 authoring
  commit (`3bdff70`), no xian follow-up. `git log --all -i --grep="ground-rules\|blank-slate"` — same single
  hit as 8/12/8/13 (my own 8/12 no-op log entry), Calliope's standing question to xian on the
  standing-default-vs-blank-slate shape still unanswered. Both confirmed still genuinely parked, not stalled
  on me.
- Read `docs/briefs/cross-pollination/current.md` (2026-08-13 brief, still current — no 8/14 brief filed
  yet) — the carried-context Key Insight already carries Theseus's 8/13 correction marker; nothing new to
  action beyond what the mail thread above already carried.

## No code changed this fire

Design decision + doc addendum + mail + coordination only. No `packages/` files touched, so no test/
typecheck/build run — nothing to verify at that layer this fire.

## Verify-before-asserting checks run this fire (not skipped as "too small to check")

- `StreamEvent` shape, `handleStreamComplete`, `fetchMessages`'s call site, `createCarriedContextArtifact`'s
  return shape, and the `streamClaudeCore`/`streamClaude` call-order wrinkle — all read directly from source
  this fire (`types.ts`, `App.tsx`, `client.ts`, `db/queries.ts`, `hooks/useStreams.ts`), not recalled from
  the 8/13 log entries or Theseus's memo's own claims about them.
- Import-confirm-step and ground-rules-UX parked status re-verified via `git log` this fire, not carried
  forward unchecked.

## Git verification (session wrap protocol) — appended after push

Pushed `claude/iris-cycle` to `origin/main`.

```
$ git log origin/main --oneline -5
```
(pasted after push, below)

Deliverable files re-`ls`'d clean before commit:
- `docs/ux/carried-context-visibility-2026-08-13.md`
- `docs/mail/iris-to-theseus-cc-daedalus-team-reload-time-gap-decided-2026-08-14.md`
- `docs/COORDINATION.md` (Iris section updated)
- `docs/logs/2026-08-14-0717-iris-log.md` (this file)

— Iris
