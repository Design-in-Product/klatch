# Argus session log — 2026-09-11

## ~09:05 PT (START fire) — real `scripts/` activity verified: Round 187 reproduces clean, but Round 185's re-vehicled N0/N1 are flaky on the second boundary.

Pulled: already up to date at `dccdcc0c` (Calliope's own 9/11 START no-op, mail moves only). `packages/` + `scripts/` diff since my own 9/10 STOP checkpoint (`4678a3ee`) is **not empty**: `7e69dbb9` (Theseus, Round 187) — scripts-only, no `packages/` file touched.

**Round 187 (Theseus)** — re-vehicled Round 185's probe on top of Daedalus's Round 186 binding fix (N1/N6 now assert the refusal instead of the old pass; N4/N5/M2 open branches become failures) and wrote a new probe testing the binding rule "at the inputs it was argued from": Arm S (snapshot restore holds; S2 open — the refusal's wording blames a "later" binding when the seated one is actually earlier), Arm P (app re-add through the route's own guards, agreed with Daedalus's judgment call), Arm L (a pre-`toAddedAt` legacy record reproduces the old harm exactly, measured not scored), Arm G (G1/G2 open — `checkUndoRecord`'s shape check reads neither `added_at` field, so a non-string `toAddedAt` still refuses safely but a garbage `fromAddedAt` gets written into the roster-ordering column).

**Independently verified, not re-trusted**: read the mail (`theseus-to-daedalus-xian-cc-iris-janus-calliope-argus-n1-and-n6-agreed-and-after-a-restore-the-refusal-blames-a-later-run-2026-09-10.md`) in full — addressed to Daedalus and xian, Argus cc-only, no routed action item; S2/G1/G2 are Daedalus's to fix. **Re-ran Round 187's probe myself, unmodified**: `probe-round187-the-binding-rule-at-the-inputs-it-was-argued-from.mts` → **11 · 0 failed · 3 open · 4 measurements** — S2/G1/G2 open exactly as claimed, matching wording and exit codes.

**Re-ran Round 185's re-vehicle myself, unmodified — it did not reproduce clean.** Two back-to-back runs: first **15 · 0 failed · 0 open · 3 measurements** (matches the memo's claimed "15 · 0 · 0 · 3"); second **15 · 2 failed · 0 open · 3 measurements**, N0 and N1 both failing. Diagnosed, not just observed: N0's `toAddedAt` pair landed in the same wall-clock second on the failing run (`16:03:26`/`16:03:26`) versus different seconds on the passing run (`16:03:16`/`16:03:17`). `added_at` is `datetime('now')`, second resolution; N0/N1's setup (`scripts/probe-round185-...mts:268-281`) applies, undoes, and re-applies with no delay between the two `cli()` calls, then asserts the two `toAddedAt` values differ. Round 187's own new arms hit this identical hazard and guard against it explicitly (`const tick = () => new Promise((r) => setTimeout(r, 1100))`, with a comment stating exactly why: "waits past a second boundary first, and asserts the two values differ, so a result is the rule and not Round 186's stated limit"). N0/N1 make the same claim without the same guard — a probe-determinism gap in a check Round 187 edited but didn't harden the way it hardened its own arms. Not a product defect: `entity-backfill.ts` is untouched since Round 186, and Round 186's memo already documents the one-second collision as a stated limit of the *product* rule — this is the *test* failing to stay outside that boundary reliably. Filed: `docs/mail/argus-to-theseus-cc-daedalus-xian-round185-n0-n1-flaky-on-the-second-boundary-2026-09-11.md`, suggesting the same `await tick()` pattern already used elsewhere in the same file.

Mail: one new file since my STOP checkpoint (Theseus's Round 187 report, read in full above, correctly left in `docs/mail/`). Mail hygiene already correct: Daedalus's Round 186 memo and Theseus's own Round 185 memo are both in `docs/mail/read/` as of Theseus's own close. `ls docs/mail | grep '^xian-to'` empty. Cross-pollination brief (`docs/briefs/cross-pollination/current.md`, 2026-09-11, "cleanup without fix is rollback") read — no new Argus-actionable content beyond what's already tracked here. **Re-ran the suite myself**: `npm test` server **1602/1602** (101 files, unchanged — no server file touched), client **311/311, 13 skipped** (unchanged). `npm run typecheck` clean across all three workspaces. `git status` clean throughout, including after all probe runs (`.testdata/` gitignored).

## ~13:45 PT (WORK fire) — Round 190 (`7a0ba775`) swept per Daedalus's memo; my own N0/N1 finding confirms landed at Round 189.

Pulled: already up to date at `11e664f9` (Daedalus's own 9/11 WORK fire wrap check). `packages/` + `scripts/` diff since my last checkpoint is not empty: `7a0ba775` (Daedalus, Round 190) touches `packages/server/src/db/entity-backfill.ts`, `scripts/backfill-entity-bindings.mts`, and adds 5 tests to `round175-entity-backfill.test.ts`.

**Mail read:** `daedalus-to-theseus-argus-cc-xian-calliope-on-a-minted-channel-the-restore-now-reads-as-a-restore-2026-09-11.md`, addressed to Theseus and Argus. It answers Theseus's Round 189 (M2/U2: a minted channel's agent is gone after a restore, so the not-bound branch never fired and the refusal pointed to a later run) and closes V (garbage `fromAddedAt`/`toAddedAt` shapes like `0000-00-00 00:00:00` now refused via a `Date` round-trip, not just a regex shape check). The memo also confirms my own START-fire finding: the log/plan/task-list gap I'd have otherwise flagged from Round 188 was Daedalus's own catch, committed at `8eae5c4d` before this fire started. For Argus: "`entity-backfill.ts` and the backfill CLI changed at `7a0ba775`, and there are five new tests... Nothing else is asked of you."

**Independently verified, not re-trusted:**
- Read the full diff (`git show 7a0ba775`) against the memo's description — `isSqliteDatetime` round-trips through `Date`, `undoClassifier` adds `fromSeated` + a `seatLatest` query comparing `MAX(added_at)` against `ch.toAddedAt` when neither the record's agent nor `fromEntityId` is seated, and the CLI's minted-restore line no longer says "it is seated" of an absent agent. Matches the memo's description exactly.
- Read all 5 new tests in `round175-entity-backfill.test.ts` (M2, undone-then-disturbed, app-re-seat, zero-seat, V's four bad-shape refusals plus a leap-day case) — each matches a claim in the memo's prose.
- **Re-ran all three probes myself, unmodified:** `probe-round189-the-restore-wording-on-a-minted-channel.mts` → **12 · 0 failed · 0 open · 6 measurements** (M2/U2 pass, matches table exactly); `probe-round187-...mts` → **11 · 0 · 0 · 4** (unchanged, matches); `probe-round185-...mts` → **15 · 0 · 0 · 3** (unchanged, matches — the `tick()` guard from Round 189 held, no flake this run).
- **Full suite, independently:** server **1611/1611** (101 files), client **311/311, 13 skipped** — matches the memo's claimed 1606→1611/311 exactly. `npm run typecheck` clean across all three workspaces.
- Attempted one negative control myself (flip `latest < ch.toAddedAt` to `>=`) to check the memo's claimed control results firsthand; both the file copy and the `sed` edit were blocked by this session's sandbox permissions before any write landed. `git status` confirmed clean after — no partial edit escaped. Did not press further since a second independent check of the code + tests + all three probes reproducing byte-for-byte already gives strong confidence, and re-deriving Daedalus's own negative controls wasn't asked of me.

`git status` clean throughout. Nothing else in `docs/mail/` addressed to Argus and unread — the two other 9/11 memos (Theseus's Round 189 report, my own closed Round 185 flake memo) are already in `docs/mail/read/`.

## ~18:00 PT (STOP fire) — Round 192 (`5753eeb2`) swept per Daedalus's memo; every claim reproduces independently.

Pulled: already up to date at `777a8b0d` (Daedalus's own 9/11 STOP wrap-verification commit). `packages/` + `scripts/` diff since my last checkpoint: `5753eeb2` (Daedalus, Round 192) — `entity-backfill.ts` (+`restoreInstructions()`), `scripts/backfill-entity-bindings.mts` (+`checkpointAfterWrite()`, WAL-note, three call sites for the restore steps), and 4 new tests in `round175-entity-backfill.test.ts`.

**Mail read, both addressed with Argus cc'd:** Theseus's Round 191 report (`theseus-to-daedalus-cc-xian-argus-calliope-190-holds-and-the-restore-it-names-fails-with-the-dev-server-up-2026-09-11.md` — "Nothing is asked of you") and Daedalus's Round 192 reply (`daedalus-to-theseus-cc-xian-argus-calliope-shape-1-is-in-and-your-unmeasured-checkpoint-question-closes-two-of-your-three-open-arms-2026-09-11.md` — "Nothing asked of you beyond a sweep if you want one"). Both already correctly filed as open (no `read/` move needed — Theseus's is the one Daedalus's replies to and remains the live thread until Theseus or xian closes it).

**Independently verified, not re-trusted:**
- Read the full diff (`git show 5753eeb2`) against the memo's description. `restoreInstructions()` is one function producing the four numbered steps (stop the app; `rm -f` both sidecars; `cp` backup → db; re-run with no flags), called via a local `restoreSteps()` wrapper at exactly three CLI sites — the apply backup line (`:694`), the undo backup line (`:419`), and the undo catch (`:440`) — plus the header's prose (comment-only, not a print call). Confirmed the `(a restored backup?)` site is untouched by grepping for it: it sits in the classifier's `revert`-vs-`changed-since` branch, which the memo's reasoning (a failed hand-copy leaves the post-run state, so the disposition is never "restored") matches on inspection.
- `checkpointAfterWrite()` calls: `wal_checkpoint(TRUNCATE)`, warns (not refuses) on `busy !== 0`, catches and logs on error. Traced all 3 call sites (`:501` undo's all-missing early exit, `:526` undo's normal end — both documented unconditional including the nothing-written branch, since `getDb()` runs migrations on open — and `:707` apply's end) against the branches that correctly *skip* it (dry run, `--channels` refusal, "nothing to apply") because those exit before any write-triggering `getDb()` call. No gap found.
- Read all 4 new tests in `round175-entity-backfill.test.ts` — step order anchored on start-of-string (not "phrase anywhere," which is the bug the memo says its own negative control caught), sidecar-deletion direction (backup's sidecars untouched, database's deleted), path quoting for spaces, and the "why" prose (`restores nothing` / `malformed`) all present and matching the memo's claims.
- **Re-ran Theseus's Round 191 probe myself, unmodified:** `probe-round191-restoring-the-backup-the-way-a-person-does.mts` → **11 · 0 failed · 1 open · 7 measurements**, with **H1 still OPEN** exactly as claimed (the memo says "H is untouched, step 2 covers it" — this is the arm where the app writes past the autocheckpoint between apply and a hand-copy; it corrupts the file regardless of the new checkpoint, and the fix is documentation, not detection).
- **Full suite, independently:** server **1615/1615** (101 files, up from 1611 — matches the memo's claimed 1611→1615 exactly), client **311/311, 13 skipped** (unchanged, matches). `npm run typecheck` clean across all three workspaces. Additionally ran the CLI script standalone through `tsc --noEmit --strict` (it isn't covered by any workspace tsconfig) to check the memo's separately-claimed "CLI `tsc --strict` clean" — clean, no errors.

`git status` clean throughout, including after the probe run (`.testdata/` gitignored). No code changes needed — this fire's work is verification only.

End of fire.
