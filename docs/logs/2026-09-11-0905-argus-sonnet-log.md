# Argus session log — 2026-09-11

## ~09:05 PT (START fire) — real `scripts/` activity verified: Round 187 reproduces clean, but Round 185's re-vehicled N0/N1 are flaky on the second boundary.

Pulled: already up to date at `dccdcc0c` (Calliope's own 9/11 START no-op, mail moves only). `packages/` + `scripts/` diff since my own 9/10 STOP checkpoint (`4678a3ee`) is **not empty**: `7e69dbb9` (Theseus, Round 187) — scripts-only, no `packages/` file touched.

**Round 187 (Theseus)** — re-vehicled Round 185's probe on top of Daedalus's Round 186 binding fix (N1/N6 now assert the refusal instead of the old pass; N4/N5/M2 open branches become failures) and wrote a new probe testing the binding rule "at the inputs it was argued from": Arm S (snapshot restore holds; S2 open — the refusal's wording blames a "later" binding when the seated one is actually earlier), Arm P (app re-add through the route's own guards, agreed with Daedalus's judgment call), Arm L (a pre-`toAddedAt` legacy record reproduces the old harm exactly, measured not scored), Arm G (G1/G2 open — `checkUndoRecord`'s shape check reads neither `added_at` field, so a non-string `toAddedAt` still refuses safely but a garbage `fromAddedAt` gets written into the roster-ordering column).

**Independently verified, not re-trusted**: read the mail (`theseus-to-daedalus-xian-cc-iris-janus-calliope-argus-n1-and-n6-agreed-and-after-a-restore-the-refusal-blames-a-later-run-2026-09-10.md`) in full — addressed to Daedalus and xian, Argus cc-only, no routed action item; S2/G1/G2 are Daedalus's to fix. **Re-ran Round 187's probe myself, unmodified**: `probe-round187-the-binding-rule-at-the-inputs-it-was-argued-from.mts` → **11 · 0 failed · 3 open · 4 measurements** — S2/G1/G2 open exactly as claimed, matching wording and exit codes.

**Re-ran Round 185's re-vehicle myself, unmodified — it did not reproduce clean.** Two back-to-back runs: first **15 · 0 failed · 0 open · 3 measurements** (matches the memo's claimed "15 · 0 · 0 · 3"); second **15 · 2 failed · 0 open · 3 measurements**, N0 and N1 both failing. Diagnosed, not just observed: N0's `toAddedAt` pair landed in the same wall-clock second on the failing run (`16:03:26`/`16:03:26`) versus different seconds on the passing run (`16:03:16`/`16:03:17`). `added_at` is `datetime('now')`, second resolution; N0/N1's setup (`scripts/probe-round185-...mts:268-281`) applies, undoes, and re-applies with no delay between the two `cli()` calls, then asserts the two `toAddedAt` values differ. Round 187's own new arms hit this identical hazard and guard against it explicitly (`const tick = () => new Promise((r) => setTimeout(r, 1100))`, with a comment stating exactly why: "waits past a second boundary first, and asserts the two values differ, so a result is the rule and not Round 186's stated limit"). N0/N1 make the same claim without the same guard — a probe-determinism gap in a check Round 187 edited but didn't harden the way it hardened its own arms. Not a product defect: `entity-backfill.ts` is untouched since Round 186, and Round 186's memo already documents the one-second collision as a stated limit of the *product* rule — this is the *test* failing to stay outside that boundary reliably. Filed: `docs/mail/argus-to-theseus-cc-daedalus-xian-round185-n0-n1-flaky-on-the-second-boundary-2026-09-11.md`, suggesting the same `await tick()` pattern already used elsewhere in the same file.

Mail: one new file since my STOP checkpoint (Theseus's Round 187 report, read in full above, correctly left in `docs/mail/`). Mail hygiene already correct: Daedalus's Round 186 memo and Theseus's own Round 185 memo are both in `docs/mail/read/` as of Theseus's own close. `ls docs/mail | grep '^xian-to'` empty. Cross-pollination brief (`docs/briefs/cross-pollination/current.md`, 2026-09-11, "cleanup without fix is rollback") read — no new Argus-actionable content beyond what's already tracked here. **Re-ran the suite myself**: `npm test` server **1602/1602** (101 files, unchanged — no server file touched), client **311/311, 13 skipped** (unchanged). `npm run typecheck` clean across all three workspaces. `git status` clean throughout, including after all probe runs (`.testdata/` gitignored).

End of fire.
