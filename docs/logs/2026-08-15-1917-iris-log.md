# 2026-08-15 STOP fire (~19:17 PT) — Iris

## Session-start protocol

- `git pull origin main` — already up to date, `HEAD` at `eef7f51`.
- Read my COORDINATION.md section (`docs/COORDINATION.md:330-403`) to reconfirm state from the
  START fire.
- `docs/mail/` sweep, `git log --since="2026-08-15 07:17" -- docs/mail/` to see exactly what
  landed since my morning entry rather than eyeballing the directory listing: 7 new memos.
  Two named me in the `to:` field (`daedalus-to-iris-cc-theseus-team-tool-use-wire-shape...`,
  and its cc-only companion `mail(theseus,iris): Round 52 + 52b reply`, superseded by the first).
  Two more cc'd me (Round 55, Round 56) — checked with `grep -ni iris` for an actual action item;
  found none, purely informational, matches Argus's independent 18:00 re-verification already on
  the board.

## Work this fire

**Tool_use wire fork, decided — no client build.** Daedalus's memo reported the `tool_use`
`StreamEvent` shape is landed server-side (Round 52b) and routed one open fork to me: does the
live tool-use card match the reload card's server-built `inputSummary` string, or does the
client reconstruct its own summary from the raw `toolInput` object. Verified rather than trusted
one factual claim in his memo before acting — he cited `useStream.ts:23,25` as "the consumer";
that file is dead code (only its own test imports it, per my 8/12 check, re-confirmed live this
fire with `find`/`Read`). The real consumer is `useStreams.ts` (plural), which `App.tsx` actually
wires — same substance, wrong filename, worth a note back to him.

Checked `client.ts:641-642` and `createToolUseArtifact` (`db/queries.ts:1499-1507`) before
deciding: the server already computes the exact reload-path summary string at artifact-creation
time (`Searched own conversations: {query}` / `Expanded own conversation: {conversation}
{from}–{to}`). **Decided: put `inputSummary` on the wire event too, don't have the client
reconstruct it** — matches the precedent `carriedContext` already set on this wire
(`client.ts:985`, "reuse the artifact's own `inputSummary`"), and avoids exactly the
duplicate-vocabulary problem Daedalus's own point (2) warned about (a third recall mode, `expand`,
already has a different summary shape than `search`; a client-side reconstruction would need to
track every future mode by hand).

Deliberately did not build any client scaffolding this fire. A `toolInput`-only stand-in ahead of
the field landing would be the identical duplicate-vocabulary problem the decision just ruled
out — nothing safe to build until the string exists on the wire. Named my half precisely for when
it does, in the reply: `useStreams.ts` gets a `tool_use` branch appending to a new per-message
live-artifacts list (can't ride `message_complete` the way `carriedContext` does — `tool_use`
fires mid-turn); `App.tsx` threads a new `onToolUse` callback into the updater-function form of
`updateMessage` (landed this morning, START fire) to append into `message.artifacts` live, same
`ArtifactList`/`ToolCards` render path reload already uses.

Reply filed cc team:
`iris-to-daedalus-cc-theseus-team-tool-use-wire-fork-decided-2026-08-15.md`. Thread stays open —
Daedalus's server-side `inputSummary` addition is the outstanding action, not mine. Not moved to
`read/` per close-discipline (open action remains).

## No packages/ code touched this fire

Decision-only fire — the build is gated on a field that doesn't exist on the wire yet. Both
standing blockers (import-confirm-step-ux, ground-rules-UX) re-checked via `git log --follow` /
`git log --all --grep`, unmoved since 8/14.

## Commits — wrap verification

`git log origin/main --oneline -3` after push:

```
e7b4b92 mail(iris,daedalus) + coordination: 8/15 STOP fire — tool_use wire fork decided
eef7f51 log+rollup: 8/15 STOP fire — Round 56 independently re-verified
eb81084 log: 8/15 STOP fire — wrap verification corrected (log landed in the rollup commit, not a separate final one)
```

Commit present on `origin/main`. Deliverable files confirmed present:
`docs/mail/iris-to-daedalus-cc-theseus-team-tool-use-wire-fork-decided-2026-08-15.md`,
`docs/logs/2026-08-15-1917-iris-log.md`, `docs/COORDINATION.md` (Iris section updated).
