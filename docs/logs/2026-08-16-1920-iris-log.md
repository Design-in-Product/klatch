# 2026-08-16 19:20 PT — Iris STOP fire

**19:20 — tool_use live card built.** Mail sweep since the 07:17 START fire: `git status`
confirmed clean sync to `origin/main`; three new memos, all read. Two (Daedalus↔Theseus on the
recall-recogniser/cross-model research track, cc'd to Iris) carry no "Iris" section and no routed
question — checked in full, informational only, left in place. The third, Daedalus's
`daedalus-to-iris-cc-theseus-team-inputsummary-is-on-the-wire-2026-08-16.md`, unblocked the one
standing item named at 07:17: `inputSummary` landed on `StreamEvent`'s `tool_use`, computed once
(`toolUseInputSummary` in `client.ts`) and shared by the emit site and `createToolUseArtifact` so
live and reload strings can't drift — verified in `packages/shared/src/types.ts` before building
against it, not taken from the memo's framing.

Built the client half I scoped in the 8/15 STOP fire:

- `packages/client/src/hooks/useStreams.ts` — new optional fourth callback
  `onToolUse(messageId, toolName, inputSummary)`, and a `tool_use` branch in the SSE `onmessage`
  handler that calls it. Does not touch `states` or close the `EventSource` — a `tool_use` event
  is mid-turn, not terminal.
- `packages/client/src/App.tsx` — new `handleToolUse`, threaded as `useStreams`'s fourth arg.
  Appends a synthesized `MessageArtifact` (`type: 'tool_use'`, same shape `fetchMessages` returns
  on reload) to the message's `artifacts` via `updateMessage`'s updater-function form — append,
  not replace, so it composes safely with the `carried_context` merge already in
  `handleStreamComplete` (safe specifically because of Theseus's round49 array-replace fix, 8/15).
- `packages/client/src/components/MessageList.tsx` — untouched. `ArtifactList`/`ToolCards`
  already read `toolName`/`inputSummary` off `tool_use` artifacts for the reload path; live and
  reload now render through the identical function.

**Tests:** 3 new in `useStreams.test.ts` — `onToolUse` fires without closing the stream, fires
once per call in order for a multi-call turn, and doesn't require `inputSummary` for tools with no
summary vocabulary (`save_file` case, matching the field's documented absent-not-undefined
contract). No new App-level test — same reason as the 8/12 STOP fire's carried-context work: no
existing App+streams integration harness to extend cheaply, and the wiring here is a one-line
callback passthrough between two already-tested layers.

**Verified, not asserted:** `npm test` → 1378 passed server (unchanged, 82 files) / 233 passed
client (+3, 13 skipped), exit 0. `npm run typecheck` clean across all three workspaces. `npm run
build` green end-to-end including a full `vite build` of `dist/`.

Reply filed and thread closed to `read/`:
`iris-to-daedalus-cc-theseus-team-tool-use-live-card-built-2026-08-16.md` (moved alongside the
inbound memo it closes, per close-discipline). COORDINATION.md Iris section updated with the full
entry. Standing blockers (import-confirm-step-ux, ground-rules-UX) unchanged — both still with
xian and Calliope's open question respectively, not re-checked via `git log` this fire since
nothing in the mail sweep touched either.

**Caveat, same as every layer of this feature:** no live turn has been driven through the running
app. Everything above is verified against the documented `StreamEvent` contract and mocked SSE
injection. Confirming a human sees the right words on the right card at the right moment is
Theseus's territory next, per Daedalus's own memo.

**Status: available.** Next: (1) import-confirm-step-ux, pending xian's review; (2) ground-rules-UX
+ direct-reply affordance, blocked on Calliope's question; (3) render-layer follow-up only if
Theseus's live probe finds a specific card-count/ordering surprise in the tool_use live card — not
a re-open of the wire/client split itself.
