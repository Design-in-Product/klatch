# Intelligence Sweep — 2026-08-03

**Filed by:** Automated External Scanner
**Curated by:** Pending Argus review
**Scope:** External sources, 7-day window (2026-07-27 → 2026-08-03)
**Type:** Automated weekly scan
**Predecessor:** `2026-07-27-sweep.md`

**Carry-over items from 7/27 sweep (open, not re-reported as new):**
- Opus 5 not in picker — open mail `docs/mail/argus-to-daedalus-opus-lineup-refresh-2026-07-05.md` now **29 days old**; see escalation note under item #2.
- Anthropic SDK TS `^0.110.0` — still 5 minors behind 0.115.0; no new release found this week; carry-over unchanged.
- DEFAULT_MODEL = `claude-opus-4-7` while Opus 5 is the API default.

---

## HIGH RELEVANCE

### 1. MCP SDK v2 Now Stable — Package Name Changes, Not a Version Bump

The MCP 2026-07-28 specification published July 28 (was RC on July 27). Alongside it, the TypeScript SDK v2 shipped stable. **Critical detail not in the 7/27 sweep:** v2 retires the monolithic `@modelcontextprotocol/sdk` package in favor of focused packages — `@modelcontextprotocol/server` (for servers) and `@modelcontextprotocol/client` (for clients). This is a package rename requiring code changes, not a semver bump.

Latest 1.x version is **1.30.0** (published ~July 28). Klatch's `^1.29.0` pin auto-resolves to 1.30.0 — no immediate breakage. But migration to v2 requires updating imports, package dependencies, and any capability registrations.

**Breaking changes in 2026-07-28 spec** (features deprecated, replaced):
- **Roots** — deprecated; replaced by tool parameters, resource URIs, or server config
- **Sampling** — deprecated; replaced by direct LLM provider API calls
- **Logging** — deprecated; for stdio transports, replaced by stderr

**Klatch exposure:** `git grep -il "sampling\|roots\|logging"` in `packages/server/src/mcp/` — **zero matches**. Klatch's MCP server does not use any of the three deprecated features. Zero breakage from protocol changes today.

**Source:** https://blog.modelcontextprotocol.io/posts/2026-07-28/; https://npmjs.com/package/@modelcontextprotocol/sdk
**Klatch impact:** No breakage today; 10-week migration window started July 28. When migrating, the `@modelcontextprotocol/sdk` import → `@modelcontextprotocol/server` rename affects `packages/server/src/mcp/`. Route to Daedalus for planning.
**Prior mentions:** Tracked since `2026-05-18-sweep.md`; "day before RC" in `2026-07-27-sweep.md`. **Frame as: spec final, package split confirmed — migration now concrete.**
**Verified against:** `packages/server/package.json:16 — "^1.29.0"` (auto-resolves to 1.30.0); `packages/server/src/mcp/` — no use of roots/sampling/logging.

---

### 2. Opus 5 Gap — Carry-Over Escalation (29 Days)

`claude-opus-5` remains absent from `AVAILABLE_MODELS` in `packages/shared/src/types.ts`. Open mail `argus-to-daedalus-opus-lineup-refresh-2026-07-05.md` is now **29 days old** with no action. `claude-opus-4-7` is still labeled `"Newest Opus — adds xhigh effort + thinking summaries"`, which is factually incorrect by two generations.

Reminder: Opus 5 launched July 24; Claude Code v2.1.219 made it its default Opus the same day. Klatch is now the only major Claude interface with no path to the current flagship model. The `DEFAULT_MODEL = 'claude-opus-4-7'` comment at `types.ts:29` says this is a manual constant by design — meaning a human decision is needed to flip it.

**Source:** Carry-over from `2026-07-27-sweep.md` item #1.
**Prior mentions:** Open mail from 7/05; `2026-07-27-sweep.md` item #1. Not a new find — **escalation only**.
**Verified against:** `packages/shared/src/types.ts:1-8` — `claude-opus-5` absent; `git grep "claude-opus-5"` returns `docs/intel/2026-07-27-sweep.md` only.

---

## MEDIUM RELEVANCE

### 3. Claude Sonnet 5 Tokenizer Note — Already in Picker, But Description Missing Warning

Claude Sonnet 5 (`claude-sonnet-5`) is already in Klatch's picker (label: `"Newest Sonnet — balanced speed and quality"`). The types.ts comment at line 19-21 notes the Opus 4.7 tokenizer produces "1×–1.35× tokens" for equivalent input. Sonnet 5 inherits the **same tokenizer** (confirmed: "~30% more tokens for the same text") but the Sonnet 5 description in `AVAILABLE_MODELS` carries no equivalent warning.

This is a discoverability gap: users selecting Sonnet 5 won't know it hits the 160K compaction threshold sooner than Sonnet 4.6. Introductory pricing ($2/$10 per MTok) through August 31 then reverts to $3/$15.

**Source:** https://platform.claude.com/docs/en/about-claude/models/whats-new-sonnet-5; https://www.anthropic.com/news/claude-sonnet-5
**Klatch impact:** Minor — Sonnet 5 already in picker. Consider adding tokenizer note to description. Low-urgency.
**Prior mentions:** `2026-07-06-sweep.md` through `2026-07-13-sweep.md` tracked Sonnet 5 launch. **Frame as: delta on description gap only.**
**Verified against:** `packages/shared/src/types.ts:4` — `claude-sonnet-5` present; no tokenizer note in description.

### 4. Claude Opus 4.1 Deprecation — August 5 (2 Days Away)

`claude-opus-4-1-20250805` retires from the Claude API on August 5, 2026. Anthropic recommends migrating to `claude-opus-4-8`. No Klatch exposure — grep confirms the model ID appears only in `docs/intel/` files, not in packages/, entities, or channels.

**Source:** https://platform.claude.com/docs/en/about-claude/model-deprecations
**Klatch impact:** Zero — `claude-opus-4-1` not in Klatch's picker or any known DB entries.
**Prior mentions:** `2026-06-08-sweep.md`, `2026-06-21-sweep-curated.md`, `2026-07-20-sweep.md`. Not a new find.
**Verified against:** `git grep claude-opus-4-1 packages/` — zero matches.

### 5. Hono 4.12.33 (July 31) — Auto-Resolves

Routine patch. Klatch's `^4.12.18` auto-resolves to 4.12.33. No action needed.

**Source:** https://www.npmjs.com/package/hono
**Prior mentions:** 4.12.32 in `2026-07-27-sweep.md`. Routine delta.
**Verified against:** `packages/server/package.json — "hono": "^4.12.18"` — auto-resolves.

---

## LOW RELEVANCE

### 6. Legacy Anthropic Workbench + Experimental Prompt Tools API Retirement — August 17
Anthropic is retiring the legacy Workbench and experimental prompt tools APIs on August 17, 2026. Klatch uses the Messages API directly via `@anthropic-ai/sdk` — the Workbench and prompt tools API are developer console/UI tooling that Klatch has never used. No action required.
**Prior mentions:** None found.

### 7. Vite 8.2.0 (July 30) — Carry-Over
Klatch pins `"vite": "^6.0.0"` (client/package.json:31). Vite 8.x (Rolldown-powered bundler) was released March 12, 2026 — tracked in 21 prior intel files. The specific 8.2.0 patch is a delta of an ongoing carry-over; not a new finding. A major-version upgrade decision is pending whenever Daedalus wants to address the build-tooling gap.
**Prior mentions:** Tracked in 21 intel files since `2026-03-24-sweep.md`. **Frame as: ongoing carry-over, minor version delta only.**

### 8. Bloome — No New Update
No product announcements found in the July 27 – August 3 window. Still in July 1 launch state.
**Prior mentions:** First found `2026-07-20-sweep.md`.

### 9. Labrador AI (Erika Flowers) — No New Activity
Still in pre-launch phase. No new public announcements.
**Prior mentions:** `docs/mail/read/memo-janus-to-calliope-labrador-research-2026-04-11.md`.

### 10. EU AI Act High-Risk Enforcement Begins August 2, 2026
High-risk AI system requirements now in force with penalties up to 7% of global annual turnover. No direct impact on Klatch — local-first, single-user tool with no public deployment.
**Prior mentions:** None found in intel docs. Background context.

---

## Scan Summary

The headline structural item this week is **MCP SDK v2's package-split model** — a concrete migration detail the 7/27 sweep didn't have. `@modelcontextprotocol/sdk` is being replaced by `@modelcontextprotocol/server` + `@modelcontextprotocol/client`, meaning migration requires code changes, not just a version bump. Klatch's current pin (`^1.29.0`) auto-resolves to the last v1.x (1.30.0) and has zero exposure to the deprecated features (roots/sampling/logging absent from Klatch's MCP server). The 10-week migration window started July 28; Argus should route timeline to Daedalus.

The **Opus 5 picker gap** is the week's most actionable carry-over. The open mail is 29 days old; the model has been the API default for 10 days. Klatch is now the only major Claude surface without a path to Opus 5. This warrant escalation above the normal carry-over rhythm.

The **Sonnet 5 tokenizer description gap** is a minor discoverability issue worth a one-line fix in `AVAILABLE_MODELS`.

---

## Items for Argus Review

| # | Item | Priority | Prior mentions | Verified | Notes |
|---|------|----------|----------------|----------|-------|
| 1 | MCP SDK v2 stable: package split `@modelcontextprotocol/sdk` → server + client | **High** | RC in 7/27 sweep — **frame as final + package-split confirmed** | `package.json:16 ^1.29.0`; no roots/sampling/logging in mcp/ | Route migration plan to Daedalus; no breakage today |
| 2 | Opus 5 picker gap — carry-over, 29 days, no action | **High** | `argus-to-daedalus-opus-lineup-refresh-2026-07-05.md` open; 7/27 item #1 | `types.ts:1-8` — confirmed absent | Escalation only; Daedalus action needed |
| 3 | Sonnet 5 tokenizer description gap (30% more tokens, no warning in picker) | **Medium** | Sonnet 5 launch tracked `2026-07-06` onward | `types.ts:4` — Sonnet 5 present, no tokenizer note | One-line description fix |
| 4 | Claude Opus 4.1 retirement August 5 | **Medium** | Prior sweeps 6/08, 6/21, 7/20 | `git grep` — zero packages/ matches, Klatch unaffected | Monitor only |
| 5 | Hono 4.12.33 / Vite 8.2.0 / Bloome no delta / EU AI Act / Legacy Workbench | **Low** | Various | Various | No action needed |
